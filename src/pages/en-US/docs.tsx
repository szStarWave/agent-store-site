import Layout from "@theme/Layout";

import DocsIndex from "../../views/DocsIndex";

/** `/en-US/docs` —— 英文文档首页（与 `/zh-CN/docs` 同理）。 */
export default function EnDocsIndex() {
  return (
    <Layout>
      <DocsIndex />
    </Layout>
  );
}
