import Layout from "@theme/Layout";

import Landing from "../../views/Landing";

/** `/zh-CN` —— 中文落地页（带前缀的规范地址）。 */
export default function ZhLanding() {
  return (
    <Layout>
      <Landing />
    </Layout>
  );
}
