---
name: {{SKILL_NAME}}
description: >-
  {{APP_NAME}}应用的HR数据查询入口。{{FEATURE_SUMMARY}}当用户询问HR数据（如员工信息、组织架构、招聘、绩效、异动等）时必须使用本Skill。语义匹配模板优先：直接覆盖时执行模板，间接覆盖时基于模板结果做聚合分析，无覆盖时降级到自行生成SQL或指标接口。注意：数仓查询工具在读取本Skill前不可见，必须先read_file本文件才能使用。
gated_mcp_servers:
  - hr_data_service_v1
---

## 应用概览

{{APP_OVERVIEW}}

## 功能模块

{{FEATURE_MODULES}}

## 问数流程

### Step 1: 理解用户意图
理解用户想查询什么HR数据（员工信息、组织架构、招聘、绩效等），识别涉及的：
- **查询目标**：要查什么数据（人数、明细、指标值等）
- **维度/分组**：按什么维度看（部门、年龄段、时间等）
- **筛选条件**：有什么过滤条件（时间范围、组织范围、身份类型等）

### Step 2: 模板匹配（优先路径）
读取 `references/templates.md`，**语义匹配**模板——理解用户意图，理解每个模板能查什么，判断模板能力是否覆盖用户需求。

匹配不是关键词命中计数，而是**能力覆盖判断**：模板的 SQL 字段、参数、功能描述共同定义了它的能力范围。用户问题涉及的维度如果在某模板的 SQL 字段中出现，该模板即具备回答此类问题的数据基础——即使 keywords 中未显式列出对应的中文关键词。

按覆盖程度判断走哪条路径：

**① 直接覆盖** — 模板 SQL 直接回答用户问题（可能只需改参数值）
→ 选该模板，进入 Step 4 解析参数

**② 间接覆盖** — 模板包含用户问的字段/表，但 SQL 本身不做这个聚合/分析
→ 选该模板作为基础数据源，进入 Step 3 做聚合/分析

**③ 无覆盖** — 模板完全不涉及用户问的数据
→ 进入 Step 3 降级路径

> 复合模板（type: composite）按 Step 描述依次执行。

### Step 3: 聚合分析或降级

> ②间接覆盖和③无覆盖在此步骤确定策略，随后统一回到 Step 4 解析参数 → Step 5 执行。

**② 间接覆盖 → 基于模板结果聚合/分析**

当模板能提供基础数据但不直接回答用户问题时，二选一：

- **SQL 改写**：在基础模板 SQL 上叠加聚合逻辑，单次 `starrocks_query` 执行
  - 适合：数据量大、聚合逻辑简单（COUNT / GROUP BY / 过滤）
  - 改写后的 SQL 仍使用原模板的参数占位符，回到 Step 4 解析

- **结果分析**：先执行基础模板拿结果，再对结果做统计分析
  - 适合：数据量小、分析逻辑灵活（占比、交叉统计、对比、Top N）
  - 基础模板的参数仍需 Step 4 解析，执行后在 Step 5 对结果做统计分析

模型自主判断走哪条路径，依据数据量预估和复杂度。

**③ 无覆盖 → 降级路径**

降级路径不使用模板参数，参数处理方式与模板路径不同：
- **指标接口** — 问题属于预置指标（率/占比/均值/趋势）：
  `list_resources`(indicator) → `read_resource`(indicators/{api_code}) 获取参数定义 → `indicator_query`
- **自行生成 SQL** — 其他情况：
  `slang_query` 术语映射 → `list_resources`(table) + `read_resource`(tables/{table_code}) 获取表结构 → 按「查询规则」生成 SQL → `starrocks_query`

### Step 4: 解析参数

> 适用于①直接覆盖和②间接覆盖路径（基于模板的参数）。③降级路径的参数处理见 Step 3。

`references/templates.md` 标注了每个参数的类型和来源，按标注解析：
- **固定值/表常量** → 从映射表替换
- **有候选值的参数** → 优先用标注的候选值做语义匹配，不确定时 `starrocks_query` 查 DISTINCT 或 `list_resources`（`dimension`）查维度列表
- **条件参数** → 根据用户意图选择分支（如问"上个月"→快照模式→T_SNAPSHOT + 日期条件）
- **需动态解析的参数**（组织/人名等）→ `starrocks_query` 查 DISTINCT → 语义匹配
- **计算参数**（日期/范围等）→ LLM 计算

### Step 5: 执行并分析
填充 SQL → `starrocks_query` 执行 → 分析结果：
- 空结果 → 检查参数/权限，告知用户可能原因
- 脱敏值（`*`/`***`/`0`/`1970-01-01`）→ 无权限字段，告知用户
- 结果行数 = LIMIT 值 → 可能截断，告知用户
- 正常结果 → 呈现数据 + 洞察
- **Step 3 ②结果分析时** → 在此步骤对结果做统计分析（分组计数、占比、排序等）

## 查询规则

自行构造或改写 SQL 时必须遵循（包括②间接覆盖的 SQL 改写路径和③降级的自行生成路径）：

1. 仅 SELECT，必须 LIMIT
2. **禁止权限控制类 WHERE** — 数仓按用户身份自动行权限过滤，WHERE 仅用于业务条件
3. 统计人数用 `COUNT(DISTINCT staff_id8)`
4. 组织查询用 `org_full_name` + `LIKE '%关键词%'`

## 可用工具

| 工具 | 用途 |
|------|------|
| `starrocks_query` | 执行 SQL、查参数候选值（DISTINCT） |
| `slang_query` | 口语→术语映射 |
| `indicator_query` | 预置指标查询（率/占比/均值/趋势） |
| `list_resources` | 列出资源（table/indicator/dimension） |
| `read_resource` | 读取资源（表结构/术语/指标定义/维度列表） |
| `get_current_user` | 获取当前用户身份信息（姓名/工号/组织等） |
| `get_current_user_data_permission` | 查数据权限范围（按需） |
