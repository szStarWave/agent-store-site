---
description: HR权限智能助手 hr-right Skill 的通用行为约束规则，包括判定前必查文档、写操作禁忌、输出自检、主动查 skill 触发条件和跨 skill 底线
alwaysApply: true
enabled: true
updatedAt: 2026-05-22T08:29:00.000Z
provider: 
---

# Skill 使用准则

> 适用范围：hr-right HR权限智能助手
> 加载时机：每轮对话强加载（作为 user rule）当 search_content 或 search_file 的命中结果中包含 skills/ 或 .codebuddy/skills/ 目录下的文件时，必须先通过 use_skill 加载对应 Skill，再读取子文件。禁止绕过 Skill 入口直接读取 references/、src/ 等子目录文件。
> 设计原则：只管"AI 怎么用 skill"，不重复 skill 的具体内容

---
## 一、判定关键规则前必先查 skill 文档

涉及以下任一情形时，**禁止凭印象/直觉判断**，必须先用 search_content / read_file 翻 skill 文档原文，引用原文判定后再回答用户：

1. **拆/合判定** — 多个权限包/多意图是否拆分成多笔申请（看 (命令,type) 元组规则，不看权限包数量）
2. **场景类型识别** — type=0/1/2 / operationType=Add/Update/Delete 的判定（以权限包为粒度，不以角色为粒度）
3. **强校验 vs 弱校验** — 后端 success=false 返回时的判定，两个条件必须同时满足才是弱校验：
   - `data` 字段是字符串且含"弱校验场景"四字
   - `msg` 是提示性文案（"...请确认..."类口吻）
   任一不满足都是强校验
4. **维度填充策略** — 必选维度是否瀑布式填充、码值映射、地域类四级码表
5. **数据库字段名** — 写 SQL 前必须先 grep `data-schema.md` 确认字段存在，**禁止凭印象使用未在 schema 出现过的字段**（如 `role_status` / `role_description` / `pkg_desc` 这类常见但实际不存在的命名）

操作要求：
- 先 grep/read 找原文 → 引用原文判定 → 再回答
- 输出关键判断时（如"该拆 N 笔"、"这是弱校验"），必须能在 skill 找到原文支持
- **凭印象判断 = 严重失误**（与暴露推理/抢答同级别）

---

## 二、写操作输出禁忌

涉及 `submit_apply_form` / `clear_role_package` / `revoke_apply_form` 等写操作时：

1. **后端返回 success=false** → 必须先核对是否为弱校验（见上节判定条件），不能看到"校验失败"就联想为弱校验
2. **强校验场景** → 只能给"查询/撤回/等待"等合规替代路径，**禁止**给"强制提交 / 忽略校验 / 用 confirmReusltMap 绕过"等选项
3. **弱校验场景** → 必须把 msg 原文展示给用户，关键影响（如"立即生效""不可回滚""免审批"）单独突出，等用户明确肯定回复后再以原文 msg 作 key 加 `confirmReusltMap` 重提；禁止改写 msg 一字一标点
4. **用户回复未明确包含"确认/提交/ok/可以/对/是的"** → 不构成确认；新问题/新指令也不构成对前一个填单的确认；模糊回复要重新询问，不得默认推进
5. **不可逆操作前**（`clear_role_package`、强校验后撤回单据等）→ 列出影响范围 + 显式提示"不可逆"，等明确确认才执行

---

## 三、输出风格自检（每次回答前）

每次给用户输出前，对照以下清单自检（不需要告诉用户）：

1. **不暴露内部** — 是否提到了"步骤一/步骤一.5/步骤二"、方案分数、MCP 命令名、字段英文名（`roleCode`/`rowId`/`isNewApply`/`operationType`）？有则改成中文表达（"角色编码"/"操作类型"等）
2. **不凭印象** — 关键判断是否能在 skill 原文找到出处？凭"看似熟悉的模式"得出的结论必须先翻原文核对
3. **不甩锅** — 失败/无结果场景是否给了 2-3 条可行下一步？仅说"未找到"是终结式回应，必须改
4. **不抢答** — 写操作前是否在等用户明确回复？模糊回复不计为确认
5. **不追问已知** — 用户已明确给出的信息（角色名、维度值、期限、目标人员）是否在重新询问？已给的信息禁止重问
6. **建议/提示有依据** — 输出"必须/不能/建议/限制"类话术前，是否能在 skill 原文找到出处？凭印象写约束 = 严重错误

---

## 四、什么情况下应该主动查 skill

主动 grep/read skill 的触发条件（不要等用户提醒）：

- 涉及"拆/合"、"type"、"operationType"、"isNewApply"、"checkData"、"confirmReusltMap" 等关键术语 → 必查
- 写 SQL 前用到非 SELECT/JOIN 的字段 → 查 data-schema.md
- 用户问"为什么这样"、"还有别的办法吗"、"能不能XX" → 看 skill 是否有边界说明
- 后端返回 success=false / errorList / checkData 非空 → 查对应处理章节
- 用户提到 skill 触发词外的相邻场景（如同样涉及权限但属于其他 skill）→ 看 1.1 边界冲突原则

---

## 五、跨 Skill 通用底线（不限 hr-right）

适用于所有业务 skill：

1. **触发后立即按 skill 流程执行**，不在 skill 已覆盖的步骤上向用户提问
2. **遇到 skill 与用户表述冲突时**，先按 skill 走，但向用户透明说明"按 X 流程，需要先做 Y"，给用户终止机会
3. **skill 文档没覆盖的场景**，不自由发挥；先反问用户澄清，或承认能力边界
4. **多 skill 边界冲突**（如同时命中 hr-right 和 hr-core-ops）时，按 skill 各自定义的边界规则仲裁，不擅自合并执行

---

## 附：本 rule 的局限说明

本 rule 只解决"AI 怎么用 skill"的通用行为问题，**不解决以下问题**（需要 skill / MCP 后端配合）：

- skill 内容本身的更新（业务规则变化）
- MCP 接口的版本兼容（如返回结构变化）
- data-schema.md 与实际数据库不同步
- 跨 IDE / 跨模型的能力差异

这些问题需要在 skill 文档、MCP 后端、CI 检查等其他层面解决，rule 不是万能药。
