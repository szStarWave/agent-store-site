---
description: 权限中台鉴权代码开发的通用行为规则，包含启动前检查、交付闭环和交互确认约束
alwaysApply: true
enabled: true
updatedAt: 2026-06-16T16:00:00.000Z
provider: 
---

## 启动前检查规则

在执行任何项目启动或重启操作之前，必须先调用 `auth-code-checker` SKILL 执行权限代码检查，检查通过后再执行启动命令，不得跳过。

触发关键词包括但不限于：启动项目、重启项目、重新启动、跑起来、起服务、start、restart、npm run dev、npm start、yarn dev、pnpm dev、开始预览、打开项目、运行项目、帮我启动、开启服务、把项目跑起来、打开预览看看、帮我运行一下项目、开始调试、预览项目、把项目跑一下、帮我启动预览、开始运行、运行起来看看。

## 交付闭环规则（最高优先级）

**无论 `auth-code-developer` 被哪个 Skill 调用（如 `page-deliver`），在返回控制权之前，必须完成以下交付动作：**

1. 校验 `.hrright/auth.config.json` 字段完整（`sysCode` / `hrclawAppId` / `operator` / `permissions`）
2. 执行推送脚本 `push-auth-sys-info.js`（`exit code = 0` 视为成功）
3. 触发 `auth-code-tester` 进行集成测试

**这不是可选的**——代码生成（步骤 3-6）只完成了鉴权集成的一半，推送和测试是另一半。跳过此步骤将导致权限项未同步到权限中台，系统部署后无法正常鉴权。

**出口自检**：返回调用方前必须输出：

> ✅ 交付闭环已完成：系统信息和 N 个菜单权限项已推送到权限中台。系统编码：`<sysCode>`。已触发集成测试。

## 交互确认规则

在执行任何涉及代码生成的 SKILL 流程时，必须严格遵守以下规则：

- **逐步暂停等待**：每个需要用户确认的步骤，必须暂停并等待用户明确回复后才能继续，不得提前进入下一步
- **禁止自行假设**：不得替用户做任何选择，包括但不限于集成模式（A/B）、权限项编码、数据范围类型、业务字段映射等
- **禁止连续推进**：禁止在同一条回复中连续完成多个需要用户确认的步骤
- **必须重新询问**：如果用户没有明确回复当前步骤，必须重新询问，不得以默认值代替或跳过

## 强制确认步骤规则

以下步骤标记为**强制确认步骤**，在任何情况下都不得跳过，必须将结果呈现给用户并等待明确回复：

| 步骤 | 确认内容 | 强制约束 |
|------|---------|---------|
| 步骤 1 完成 | 项目扫描结果（技术栈、表结构、菜单/按钮列表） | 用户确认前不得进入步骤 2 |
| 步骤 2.3.2 | 数据范围过滤完整方案（过滤维度 + 数据库字段 + 过滤方式，一轮确认） | 用户确认前不得进入步骤 2.4 |
| 步骤 2.5 | 集成方案汇总（权限项、树形结构、sysCode、数据范围） | 用户确认前不得生成任何代码或文件 |
| 步骤 8.2 | 权限项树形绑定关系（菜单 → 按钮归属） | 即使上下文中已有相关结果，也必须重新展示给用户确认，不得基于记忆或推断自动决定 |

**通用约束**：上述每个强制确认步骤输出后必须立即停止，禁止在同一轮回复中同时输出确认内容和后续步骤的执行结果。

## 超管静态鉴权文件安全规则

超管静态鉴权文件（`~/.hrright/{sysCode}/local-permissions.json`、`~/.hrright/{sysCode}/local-data-scopes.json`）**仅允许在本地开发环境生成**，严禁在任何服务器环境（包括但不限于 AnyDev、生产服务器、测试服务器、CI/CD 环境）上创建或写入。

- **检测到当前环境为服务器/远程环境时**（如用户提及 AnyDev、部署环境、服务器、远端机器等），必须拒绝生成超管静态鉴权文件，并提示：

  > ⛔ 超管静态鉴权文件只能在**本地开发环境**的用户 Home 目录下创建，严禁在服务器（包括 AnyDev）上生成。
  > 在服务器上存在此文件会导致所有用户绕过真实鉴权，产生严重安全风险。
  > 如需在服务器上调试权限，请联调真实的权限中台接口。

- **生成 `init-local-auth.js` 等初始化脚本时**，脚本内必须包含环境检测逻辑，在非本地环境下自动跳过文件创建（不报错，静默跳过）

## 权限层级术语规则

在与用户的任何交互（说明、提示、确认框、选项卡片等）中，**禁止单独使用 P1/P2/P3/P4 缩写**，必须使用完整的中文描述：

| 禁止写法 | 必须写法 |
|---------|---------|
| P1 | P1 菜单权限 |
| P2 | P2 按钮权限 |
| P3 | P3 API 守卫 |
| P4 | P4 数据范围 |

在代码注释、技术文档中可保留 P1-P4 缩写，但面向用户的输出必须使用完整描述。

## hrclawAppId 双向同步规则

`auth.config.json` 中的 `hrclawAppId` 字段与项目部署状态文件 `.deploy-state.json` 中的顶层 `project_id` 字段一一对应。**无论两个文件的生成先后顺序如何**，必须保证最终 `auth.config.json` 中的 `hrclawAppId` 能够获取到与 `.deploy-state.json` 一致的非空值。

### 字段对应关系

| 来源文件 | 来源字段 | 目标文件 | 目标字段 |
|---------|---------|---------|---------|
| `.deploy-state.json` | 顶层 `project_id` | `.hrright/auth.config.json` | `hrclawAppId` |

### 双向同步触发时机

**场景 1：生成 `auth.config.json` 时（鉴权集成流程，由 `auth-code-developer` SKILL 触发）**

- **若 `.deploy-state.json` 已存在**：必须读取并解析该文件，取顶层 `project_id` 字段值，写入新生成的 `auth.config.json` 的 `hrclawAppId` 字段
- **若 `.deploy-state.json` 不存在**：`hrclawAppId` 暂存为空字符串 `""`，等待场景 2 触发时反向回填

**场景 2：生成 `.deploy-state.json` 时（页面发布/部署流程触发）**

- **若 `auth.config.json` 已存在且 `hrclawAppId` 为空或缺失**：必须读取本次生成的 `.deploy-state.json` 顶层 `project_id`，反向回写到 `auth.config.json` 的 `hrclawAppId` 字段
- **若 `auth.config.json` 已存在且 `hrclawAppId` 非空**：检查其值是否与 `.deploy-state.json` 的 `project_id` 一致；不一致时以 `.deploy-state.json` 为准强制覆盖（`.deploy-state.json` 是部署平台权威来源）
- **若 `auth.config.json` 不存在**：本次仅生成 `.deploy-state.json`，待后续鉴权集成流程触发场景 1 时再写入 `hrclawAppId`
- **若本场景实际更新了 `auth.config.json` 中的 `hrclawAppId`（包括首次回填和强制覆盖两种情况）**：必须立即调用 `auth-code-developer` SKILL 的**步骤 9（推送系统信息和菜单权限项定义到权限中台）**，将最新的 `sysCode`、`hrclawAppId`、`operator`、`permissions` 重新推送到权限中台的测试环境与生产环境（`POST /api/ai/auth/saveAiAppSysInfo`），两端均返回 `success=true && code="0"` 才算同步完成；推送失败按步骤 9 的重试与中止策略处理，不得跳过
- **若本场景未实际改写 `hrclawAppId`**（值已一致或 `auth.config.json` 不存在）：跳过步骤 9 的重新推送

### 强制约束

| # | 约束 |
|---|------|
| 1 | **顺序无关**：两个文件的生成先后顺序不得影响 `hrclawAppId` 的最终一致性，先生成方需为后生成方"留位"，后生成方必须主动回填 |
| 2 | **双向幂等**：场景 1、场景 2 可多次重复执行，结果必须保持一致；不得因重复触发产生不同的 `hrclawAppId` |
| 3 | **权威来源**：`.deploy-state.json` 顶层 `project_id` 是 `hrclawAppId` 的唯一权威来源，禁止由 SKILL 自行生成或猜测 `hrclawAppId` 值 |
| 4 | **取值约束**：必须取 `.deploy-state.json` 的**顶层** `project_id`，禁止取 `steps` 节点内嵌套的 `project_id` |
| 5 | **空值兜底**：任一环节读取失败、文件缺失或字段为空时，`hrclawAppId` 暂存为空字符串 `""`，不阻断当前流程，等待对方文件生成时回填 |
| 6 | **回填即时性**：场景 2 触发反向回填时，必须在 `.deploy-state.json` 写入完成的同一轮操作中立即更新 `auth.config.json`，不得延迟到下一次会话 |
| 7 | **最小化修改**：反向回填 `auth.config.json` 时仅更新 `hrclawAppId` 字段，不得覆盖 `sysCode`、`operator`、`createdAt`、`permissions` 等其他字段 |
| 8 | **回填即重推**：场景 2 中只要实际改写了 `auth.config.json` 的 `hrclawAppId`（首次回填或强制覆盖），必须在同一轮操作内调用 `auth-code-developer` SKILL 步骤 9，向权限中台测试与生产环境重新推送系统信息与权限项；两端推送均成功才视为同步完成，不得遗漏或延迟 |


