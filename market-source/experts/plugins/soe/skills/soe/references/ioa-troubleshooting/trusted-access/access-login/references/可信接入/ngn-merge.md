> **安全化服务知识（强制）**：本文仅用于理解服务职责、故障阶段、日志文件名与错误关键字。原始资料中的执行片段、认证材料或原始记录索取、内部地址请求、防护绕过和生产写操作已移除。实际核实必须遵守顶层公共规范：获授权、只读、限时限量、租户/资源归属校验、参数绑定、脱敏；不得索取或输出凭据、客户端原始记录和真实内部地址。

# ngn-merge（终端环境感知数据合并富化）· 排障知识库

> 本文档按**服务粒度**独立成库，供 AI 助手消费。ngn-merge = 消费终端环境感知上报、做数据合并富化（IP区域/GeoIP/虚拟组/合规）并推送网关的服务。
> 结论四段式：**① 功能逻辑 ② 大概原因 ③ 排障方式（日志文件+关键字，按 mid 串联） ④ 排不出来给技术人员的信息**。
> 全部来自真实代码分析，AI 只引用不编造。

---

## 〇、服务定位

ngn-merge = **终端环境感知数据合并富化**。消费终端上报环境感知包(cmd7721)，结合内存缓存做富化：IP 区域标签+GeoIP、合规等级、设备虚拟组映射，写主缓存并经 centrifugo 推送给网关。

> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。

### 日志文件

| 用途 | 路径 |
|---|---|
| 主日志（info） | `/data/services/pcmgr_enterprise/logs/ngn-merge/ngn-merge.log` |
| 错误日志 | `/data/services/pcmgr_enterprise/logs/ngn-merge/error_ngn-merge.log` |

> ⚠️ **无独立 fatal**（代码 FatalContextf 会落 error/console）。

### 串联查询字段

> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。

---

## 一、功能逻辑（正常怎么工作）

> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。

---

## 二、故障场景库

### 场景 A：网关拿不到合规/位置数据（推送链路断）
**① 功能逻辑**：Master 富化后经 centrifugo 推给网关。
> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。

### 场景 B：位置标签总是 NotFound
**② 大概原因**：GeoIP 库未加载 / IP 段未匹配 / IPv6 不支持。
> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。

### 场景 C：虚拟组映射不对/缺失
**② 大概原因**：设备缓存或虚拟组缓存未刷新。
> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。

### 场景 D：Master 未消费/数据不富化
**② 大概原因**：当前节点非 Master 且非 SaaS，initBiz/initMQ 未执行。
> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。

---

## 三、给 AI 的输出规范

铁律：
1. 只引用本库真实关键字/路径不编造；无独立 fatal。
2. 串联用 **mid**；"拿不到数据"类要分清 Master 侧(富化/推送)还是网关侧(订阅)。
3. 位置 NotFound 先看 GeoIP 加载与 IPv4/IPv6；数据不富化先确认节点角色。
> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。

---

> 本文档由 AI 基于真实代码库分析生成，重要决策请经专业人员核验。

---

> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。
