> **安全化服务知识（强制）**：本文仅用于理解服务职责、故障阶段、日志文件名与错误关键字。原始资料中的执行片段、认证材料或原始记录索取、内部地址请求、防护绕过和生产写操作已移除。实际核实必须遵守顶层公共规范：获授权、只读、限时限量、租户/资源归属校验、参数绑定、脱敏；不得索取或输出凭据、客户端原始记录和真实内部地址。

# dlp-server（DLP 主服务·审批发起）· 排障知识库

> 本文档按**服务粒度**独立成库，供 AI 助手消费。dlp-server = DLP 主服务（外发/审批发起）。
> 结论四段式：**① 功能逻辑 ② 大概原因 ③ 排障方式（按 CheckID 串联） ④ 排不出来给技术人员的信息**。
> 全部来自真实代码分析，AI 只引用不编造。

---

## 〇、服务定位

dlp-server = **DLP 主服务**。接收 DLP 外发/审批请求，向企微发起审批(含查直属 Leader 作审批人)，落库审批记录。

### 日志文件

| 用途 | 路径 |
|---|---|
| 主日志 | `/data/services/pcmgr_enterprise/logs/dlp-server/dlp-server.log` |
| 错误 | `.../dlp-server/error_dlp-server.log` |
| fatal | `.../dlp-server/fatal_dlp-server.log` |

### 串联查询字段：CheckID/reqId、tenantID、uid。

---

## 一、功能逻辑：收 DLP 外发/审批请求→查直属 Leader 作审批人→InitiateApproval 向企微发起审批→落库。

---

## 二、故障场景库

### 场景 A：审批发不出
**② 大概原因**：企微接口/参数异常。
> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。

### 场景 B：查不到审批人
> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。

### 场景 C：审批转圈无结果
> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。

---

## 三、给 AI 的输出规范

> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。

---

> 本文档由 AI 基于真实代码库分析生成，重要决策请经专业人员核验。

---

> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。


2. 拉日志（DLP 运行链 conn→dispatch→security-engine→server，外发转圈先看 dispatch）：
> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。


---

## 相关数据表（供 AI 分析/生成 SQL）

> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。

- **data_security_policy** / **data_security_policy_to_behavior_condition**（数据安全策略+行为条件，审批策略源头）
- **dlp_approval** / **dlp_approval_record**（DLP审批/记录）
- **dlp_data_level** / **dlp_data_category** / **dlp_data_rule**（数据分级/分类/规则）
- **dlp_policy_item** / **dlp_file_source**（DLP策略项/文件来源）
> DLP主服务，审批发起。运行链 dlp-conn→dispatch→security-engine→server。告警看 dlp_alarm（见 dlp-alarm 页）。