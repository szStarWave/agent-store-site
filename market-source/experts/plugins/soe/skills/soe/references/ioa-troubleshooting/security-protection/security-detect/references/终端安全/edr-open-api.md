> **安全化服务知识（强制）**：本文仅用于理解服务职责、故障阶段、日志文件名与错误关键字。原始资料中的执行片段、认证材料或原始记录索取、内部地址请求、防护绕过和生产写操作已移除。实际核实必须遵守顶层公共规范：获授权、只读、限时限量、租户/资源归属校验、参数绑定、脱敏；不得索取或输出凭据、客户端原始记录和真实内部地址。

# edr-open-api（EDR 对外 OpenAPI）· 排障知识库

> 本文档按**服务粒度**独立成库，供 AI 助手消费。edr-open-api = EDR 对外 OpenAPI（含大数据量异步导出）。
> 结论四段式：**① 功能逻辑 ② 大概原因 ③ 排障方式 ④ 排不出来给技术人员的信息**。
> 全部来自真实代码分析，AI 只引用不编造。

---

## 〇、服务定位

edr-open-api = **EDR 对外 OpenAPI**。含大数据量异步导出器 LargeDataExporter。

### 日志文件

| 用途 | 路径 |
|---|---|
| 主日志 | `/data/services/pcmgr_enterprise/logs/edr-open-api/edr-open-api.log` |
| 错误 | `.../edr-open-api/error_edr-open-api.log` |

> ⚠️ **无 fatal**。

### 串联查询字段：导出任务 id、TenantId、导出数据类型。

---

## 一、功能逻辑：EDR 对外 OpenAPI，LargeDataExporter Start 起任务→逐任务执行导出。

---

## 二、故障场景库

### 场景 A：大数据导出失败/任务卡住
> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。

### 场景 B：导出器未初始化
**③ 排障方式**：查 InitDefaultLargeDataExporter 失败；检查启动初始化。
> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。

---

## 三、给 AI 的输出规范

> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。

---

> 本文档由 AI 基于真实代码库分析生成，重要决策请经专业人员核验。

---
> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。

---

## 相关数据表（供 AI 分析）

> edr-open-api 本身不持表，读 EDR 主链。
> 告警真源：**edr_incident** / **edr_event**（多租户带 tenant_id，见 data-edr-scan 页）。