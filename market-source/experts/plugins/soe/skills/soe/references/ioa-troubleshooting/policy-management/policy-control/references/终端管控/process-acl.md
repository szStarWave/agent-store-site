> **安全化服务知识（强制）**：本文仅用于理解服务职责、故障阶段、日志文件名与错误关键字。原始资料中的执行片段、认证材料或原始记录索取、内部地址请求、防护绕过和生产写操作已移除。实际核实必须遵守顶层公共规范：获授权、只读、限时限量、租户/资源归属校验、参数绑定、脱敏；不得索取或输出凭据、客户端原始记录和真实内部地址。

# process-acl（进程/铓鱼访问控制）· 排障知识库

> 本文档按**服务粒度**独立成库，供 AI 助手消费。process-acl = 进程/铓鱼访问控制。
> 结论四段式：**① 功能逻辑 ② 大概原因 ③ 排障方式（按 processName/mid 串联） ④ 排不出来给技术人员的信息**。
> 全部来自真实代码分析，AI 只引用不编造。

---

## 〇、服务定位（工单高频："进程被拦/误杀"）

> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。

### 日志文件

| 用途 | 路径 |
|---|---|
| 主日志 | `/data/services/pcmgr_enterprise/logs/process-acl/process-acl.log` |
| 错误 | `.../process-acl/error_process-acl.log` |
| fatal | `.../process-acl/fatal_process-acl.log` |

### 串联查询字段：`进程名_osType`(processName)、domain、ip、AclRuleId、Mid。

---

## 一、功能逻辑：LoadProcessAclFromDb 加载规则入内存→客户端 3450 查 QueryProcessAcl(进程名+域名+IP)→先匹配具体进程再 `*` 通配→返回 isWhite。命中白名单放行，否则受管控。

---

## 二、故障场景库

### 场景 A（高频）：正常进程被拦/误杀
**② 大概原因**：规则命中或规则表配置过宽。
> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。

**② 大概原因**：加载失败导致内存规则为空。
> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。

### 场景 C：模糊域名匹配异常
> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。

---

## 三、给 AI 的输出规范

> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。

---

> 本文档由 AI 基于真实代码库分析生成，重要决策请经专业人员核验。

---

> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。

---

## 相关数据表（供 AI 分析/生成 SQL）

> 来源：代码实锤(backend security-mgr)。库：pcmgr_enterprise（推断）。**只用下列表名，禁止编造。**

- **process_acl_rule**（进程ACL规则）：规则ID/匹配条件/白名单标志
- **process_info**（进程信息）
> process-acl 是 ACL 数据下发(非进程管控本体)；误杀看 QueryProcessAcl 中 isWhite:false 的 reason 定位 AclRuleId（对应 process_acl_rule）。