> **安全化服务知识（强制）**：本文仅用于理解服务职责、故障阶段、日志文件名与错误关键字。原始资料中的执行片段、认证材料或原始记录索取、内部地址请求、防护绕过和生产写操作已移除。实际核实必须遵守顶层公共规范：获授权、只读、限时限量、租户/资源归属校验、参数绑定、脱敏；不得索取或输出凭据、客户端原始记录和真实内部地址。

# conn-sc（客户端短连接接入服务）· 排障知识库

> 本文档按**服务粒度**独立成库，供 AI 助手消费。conn-sc = iOA 客户端短连接接入网关。
> 结论四段式：**① 功能逻辑 ② 大概原因 ③ 排障方式（日志文件+关键字，按 ClientMid 串联） ④ 排不出来给技术人员的信息**。
> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。

---

## 〇、服务定位

> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。

- 三个入口：`/register.cgi`、`/cmd.cgi`、`/data_report.cgi`（统一由 CgiCmd 处理）。
> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。

### 日志文件

| 用途 | 路径 |
|---|---|
| 主日志（info） | `/data/services/pcmgr_enterprise/logs/conn-sc/conn-sc.log` |
| 错误日志 | `/data/services/pcmgr_enterprise/logs/conn-sc/error_conn-sc.log` |
| fatal | `/data/services/pcmgr_enterprise/logs/conn-sc/fatal_conn-sc.log` |

> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。


---

## 一、功能逻辑（正常怎么工作）

> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。

---

## 二、故障场景库

### 场景 A：客户端请求被限流

**① 功能逻辑**：conn-sc 对出站/上报请求做启发式 QPS 限流，超阈值即拒。
**② 大概原因**：出站或上报 QPS 超过配置阈值（MaxOutRate/MaxInRate）。
**③ 排障方式**：在 `conn-sc.log` 搜 `HeuristicLimiter`（含 `collect inQps/outQps/limit-inQps/limit-outQps`）判断限流水位，命中返回 `RateLimitErrCode`。
> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。

### 场景 B：客户端鉴权失败（403）

**① 功能逻辑**：conn-sc 用 mid 查设备注册状态做鉴权，`MidPermit=false` 即拒。
**② 大概原因**：mid 未授权 / 设备状态异常 / 设备未注册。
> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。

### 场景 C：请求解析失败

**① 功能逻辑**：conn-sc 解析请求头（Content-Seq/cmd/mid），非法即拒。
> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。
**③ 排障方式**：在 `error_conn-sc.log` 搜 `client request parse error`，看行内 `error=` 具体串：`The Content-Seq is missing!` / `The cmd is missing!` / `The cmd is invalid!` / `The mid is illegal!` / `The request is invalid!` / `The device info is invalid!`。
> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。

### 场景 D：后端调用超时 / 错误（104 / 103）

> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。

---

## 三、给 AI 的输出规范

命中场景后固定四段：**【功能逻辑】【大概原因】【排障方式】【排不出来给技术人员的信息】。

> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。

---

> 本文档由 AI 基于真实代码库分析生成，重要决策请经专业人员核验。

---

> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。

---

## 相关数据表（供 AI 分析）

> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。
- 只写一张日志表 **log_conn_sc**（接入日志）
> 设备/账号真源在 devices / accounts 表（见 conn-mgr / ou-query 页）。
