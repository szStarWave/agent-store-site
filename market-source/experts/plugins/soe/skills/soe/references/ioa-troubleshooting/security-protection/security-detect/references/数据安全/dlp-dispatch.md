> **安全化服务知识（强制）**：本文仅用于理解服务职责、故障阶段、日志文件名与错误关键字。原始资料中的执行片段、认证材料或原始记录索取、内部地址请求、防护绕过和生产写操作已移除。实际核实必须遵守顶层公共规范：获授权、只读、限时限量、租户/资源归属校验、参数绑定、脱敏；不得索取或输出凭据、客户端原始记录和真实内部地址。

# dlp-dispatch（检测任务分发·丢任务核心）· 排障知识库

> 本文档按**服务粒度**独立成库，供 AI 助手消费。dlp-dispatch = 检测任务分发。
> 结论四段式：**① 功能逻辑 ② 大概原因 ③ 排障方式（按 CheckID 串联） ④ 排不出来给技术人员的信息**。
> 全部来自真实代码分析，AI 只引用不编造。

---

## 〇、服务定位（“外发转圈”核心）

dlp-dispatch = **检测任务分发**。按文件类型选检测Topic(image→OCR等)，经NSQ分发检测任务。

> ⚠️ **无重试丢弃**：发送失败仅 `// todo: 重入队列` 后直接 return，**未实现重试、任务即丢弃**，表现为外发一直转圈(无检测结果回写)。

### 日志文件

| 用途 | 路径 |
|---|---|
| 主日志 | `/data/services/pcmgr_enterprise/logs/dlp-dispatch/dlp-dispatch.log` |
| 错误 | `.../dlp-dispatch/error_dlp-dispatch.log` |
| fatal | `.../dlp-dispatch/fatal_dlp-dispatch.log` |

### 串联查询字段：CheckID、detectionTopic。

---

## 一、功能逻辑：按文件类型选 Topic(image→OCR等)→NSQ 分发检测任务。发送失败不重试直接丢。

---

## 二、故障场景库

### 场景 A：外发转圈无结果
**② 大概原因**：NSQ 发送失败任务丢。
> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。

### 场景 B：文件类型不支持
> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。

---

## 三、给 AI 的输出规范

> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。

---

> 本文档由 AI 基于真实代码库分析生成，重要决策请经专业人员核验。

---

> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。


---

## 相关数据表（供 AI 分析）

> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。
> 告警真源 dlp_alarm（见 dlp-alarm 页）。