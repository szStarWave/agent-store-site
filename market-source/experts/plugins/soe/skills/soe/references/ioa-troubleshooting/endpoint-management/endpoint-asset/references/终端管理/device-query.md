> **安全化服务知识（强制）**：本文仅用于理解服务职责、故障阶段、日志文件名与错误关键字。原始资料中的执行片段、认证材料或原始记录索取、内部地址请求、防护绕过和生产写操作已移除。实际核实必须遵守顶层公共规范：获授权、只读、限时限量、租户/资源归属校验、参数绑定、脱敏；不得索取或输出凭据、客户端原始记录和真实内部地址。

# device-query（设备信息查询·读取侧）· 排障知识库

> 本文档按**服务粒度**独立成库，供 AI 助手消费。device-query = 设备信息对外读接口（查询/缓存/心跳）。
> 结论四段式：**① 功能逻辑 ② 大概原因 ③ 排障方式（按 mid 串联） ④ 排不出来给技术人员的信息**。
> 全部来自真实代码分析，AI 只引用不编造。

---

## 〇、服务定位（工单高频："设备查不到/详情为空"）

> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。

> 区分：device-info=上报写入 / device-query=对外读取+缓存 / device-profile=只读库设备画像。

### 日志文件

| 用途 | 路径 |
|---|---|
| 主日志（info） | `/data/services/pcmgr_enterprise/logs/device-query/device-query.log` |
| 错误日志 | `/data/services/pcmgr_enterprise/logs/device-query/error_device-query.log` |
| fatal | `/data/services/pcmgr_enterprise/logs/device-query/fatal_device-query.log` |

### 串联查询字段

`mid`、`caller`(调用方)、`cost(ms)`。

---

## 一、功能逻辑（正常怎么工作）

> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。

---

## 二、故障场景库

### 场景 A：设备查不到/详情为空（高频）
> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。

### 场景 B：缓存数据异常/反序列化失败
> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。


---

## 三、给 AI 的输出规范

> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。

---

> 本文档由 AI 基于真实代码库分析生成，重要决策请经专业人员核验。

---

> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。

---

## 相关数据表（供 AI 分析/生成 SQL）

> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。

- **devices**（终端主表）：`mid`、`guid`、`name`、`group_id`、`ip`、`status`、`online_status`、`conn_active_time`、`version`、`ostype`
- **device_info**（终端详情宽表）：`mid`、`computername`、`compliance_inspect_status`、`vulcount`、`riskcount` 等
- **groups**（终端分组树）：`id`、`parent_id`、`name`、`id_path`、`ostype`
> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。
