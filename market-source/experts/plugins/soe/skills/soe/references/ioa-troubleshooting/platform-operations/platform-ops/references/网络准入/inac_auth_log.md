> **安全化服务知识（强制）**：本文仅用于理解服务职责、故障阶段、日志文件名与错误关键字。原始资料中的执行片段、认证材料或原始记录索取、内部地址请求、防护绕过和生产写操作已移除。实际核实必须遵守顶层公共规范：获授权、只读、限时限量、租户/资源归属校验、参数绑定、脱敏；不得索取或输出凭据、客户端原始记录和真实内部地址。

# inac_auth_log（准入认证日志处理+MAC黑名单加载）· 排障知识库

> 本文档按**服务粒度**独立成库，供 AI 助手消费。inac_auth_log = 准入认证日志处理 + MAC 黑名单加载。位于 Ent_Server。
> 结论四段式：**① 功能逻辑 ② 大概原因 ③ 排障方式（按 mac+nasip 串联） ④ 排不出来给技术人员的信息**。
> 全部来自真实代码分析，AI 只引用不编造。

---

## 〇、服务定位

> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。

### 日志文件

| 用途 | 路径 |
|---|---|
| 主日志 | `/data/services/pcmgr_enterprise/logs/inac_auth_log.log` |
| 采集源 | `/data/services/pcmgr_enterprise/public/tools/freeradius/log/radius*/lradius*.0` |

### 串联查询字段：mac、nasip、ClientName/MachineName、LoginTime。上报统一 NRepType=802.1x/Reason=DOT1X_REJECT。

---

## 一、功能逻辑：黑名单定时加载 + filebeat 采 freeradius 失败日志→解析 EAP 失败行→NSQ 上报 DOT1X_REJECT。

---

## 二、故障场景库

### 场景 A：802.1x 认证失败但准入日志/审计里查不到失败记录
**② 大概原因**：filebeat 未采到 radius 日志(路径不符)；行不含 Login 被过滤；字段数<18 解析失败；已被 access_svr 应答(含 accessret:0)主动不上报；非 EAP(MAB)不走此路径。
> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。

### 场景 B：MAC 黑名单加了不生效
**② 大概原因**（已核实）：本服务 checkMacBlack 已实现但**全仓无调用点**，即黑名单加载后并未在上报流程实际过滤；真正的准入黑名单拦截在 access_svr(IsInGlobalBlackList)。
> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。

### 场景 C：日志入库/上报失败
> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。

---

## 三、给 AI 的输出规范

> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。

---

> 本文档由 AI 基于真实代码库分析生成，重要决策请经专业人员核验。

---
> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。

---

## 相关数据表（供 AI 分析）

> 来源：代码实锤(inac_auth_log/src)。库：pcmgr_enterprise（推断）。**只用下列表名。**
- **inac_mac_black_list**（MAC黑名单）：`id`、`mac`、`remark`、`create_date`
- **inac_access_info**（读，认证日志，见 inac_macs 页）
> 准入认证日志/MAC黑名单。