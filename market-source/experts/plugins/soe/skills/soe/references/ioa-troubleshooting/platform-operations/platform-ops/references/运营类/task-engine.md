> **安全化服务知识（强制）**：本文仅用于理解服务职责、故障阶段、日志文件名与错误关键字。原始资料中的执行片段、认证材料或原始记录索取、内部地址请求、防护绕过和生产写操作已移除。实际核实必须遵守顶层公共规范：获授权、只读、限时限量、租户/资源归属校验、参数绑定、脱敏；不得索取或输出凭据、客户端原始记录和真实内部地址。

# task-engine（任务引擎 cron 调度）· 排障知识库

> 本文档按**服务粒度**独立成库，供 AI 助手消费。task-engine = 任务引擎（cron 定时调度生任务）。
> 结论四段式：**① 功能逻辑 ② 大概原因 ③ 排障方式（按 policyId 串联） ④ 排不出来给技术人员的信息**。
> 全部来自真实代码分析，AI 只引用不编造。

---

## 〇、服务定位

task-engine = **任务引擎**。基于 cron 定时扫描后台策略，到点调 AddTaskAndRecord 生任务并记录。

### 日志文件

| 用途 | 路径 |
|---|---|
| 主日志 | `/data/services/pcmgr_enterprise/logs/task-engine/task-engine.log` |
| 错误 | `.../task-engine/error_task-engine.log` |
| fatal | `.../task-engine/fatal_task-engine.log` |

### 串联查询字段：policyId、version、startTime/endTime。

---

## 一、功能逻辑：cron 定时扫描后台策略→到点 AddTaskAndRecord 生任务。

---

## 二、故障场景库

### 场景 A：策略加载/解析失败
> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。

### 场景 B：cron 未生成/时间段非法
> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。

### 场景 C：加任务失败
> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。

---

## 三、给 AI 的输出规范

> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。

---

> 本文档由 AI 基于真实代码库分析生成，重要决策请经专业人员核验。

---
> [安全化移除] 原始资料中的执行片段、敏感材料索取或变更步骤已删除；仅可按公共规范进行只读、限时、脱敏核实。

---

## 相关数据表（供 AI 分析）

> 来源：代码实锤(backend task-mgr)。**只用下列表名。**
- **task_seq** / **task_seq_mid** / **task_result**（见 task-server 页）
- **backend_policy** / **backend_policy_scope** / **backend_policy_record**（后台任务策略/范围/记录）
> 任务引擎 cron 调度；按 task_id 串联。