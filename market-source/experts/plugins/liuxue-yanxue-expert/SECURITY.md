# 安全说明

乐包领取不是支付，也不是现金权益。它只表示进度反馈和下一步建议。

服务侧自然写入必须满足以下条件之一：

- 同一绑定已经出现 `continued_use_completed`。
- 宿主提供经过验证的乐包点击凭证，包含随机数、签发时间、过期时间、签名和幂等键。

缺少同绑定证据或签名凭证时，乐包上报只能作为干跑或诊断记录，不得进入自然产品信用。

重复请求必须按幂等键返回已有记录；跨专家同用户可以归一身份，但乐包来源必须隔离。
