---
name: form-collection
description: "根据用户场景设计题目并创建 WPS 智能表单，用户确认后发布并返回填写链接。 当用户提到「报名表单」、「信息收集」、「登记表单」、「问卷」、「收集信息」时使用。
"
homepage: https://www.kdocs.cn/latest
version: 2.6.4
metadata: {"openclaw":{"category":"kdocs","emoji":"📋"}}
---

# 智能表单收集

智能表单收集技能根据需求设计题目、创建草稿，并在确认后发布填写链接。

> 本技能依赖 `kdocs` 技能的基础文档操作能力（认证、文件管理等），请确保已安装该技能。详见 `references/core/` 目录。

## 严格规则

### 禁止（NEVER）

- 权限不足时禁止重试或绕过，立即告知用户无权限

---

## 能力范围

### 通用工具总览

#### 草稿管理
| 工具 | 用途 |
|------|------|
| [`form.lite.create_draft`](references/form/create_draft.md) | 创建智能表单草稿 |
| [`form.lite.update_draft`](references/form/update_draft.md) | 更新智能表单草稿 |
| [`form.lite.publish_draft`](references/form/publish_draft.md) | 发布智能表单草稿 |

#### 表单查询
| 工具 | 用途 |
|------|------|
| [`form.lite.get_form_info`](references/form/get_form_info.md) | 获取智能表单信息 |

### 详细参考

| 文档类型 | 参考文件 | 说明 |
|----------|----------|------|
| WPS 智能表单 | `references/form.md` | 表单草稿创建、更新、查询与发布 |

---

## 操作指南

### 通用操作路由

| 意图 | 路由 |
|------|------|
| 读取文档内容 | `read_file`（统一入口，按后缀自动返回 Markdown 或结构化数据） |
| 创建/写入 | 新建并写入、上传本地文件、新建空白文档 → **见下方「创建/写入」** |
| 局部更新 | 改块/改段/改单元格，已有目标文档上的修改 → 按「支持的文档类型」→ 对应 reference |
| 类型专属能力 | 条件格式、导出转换、翻译、PDF 拆分、幻灯片主题、数据校验 | 按「支持的文档类型」→ 对应 reference 中的专属功能章节 |

### 高频流程指引



---

## 错误速查

| 错误特征 | 原因 | 处理方式 |
|----------|------|----------|
| `403` / 权限不足 / `无权访问` / `forbidden` | 当前凭据对目标文档、目录或资源无操作权限 | 停止操作，禁止重试或尝试其他接口绕过；告知用户当前账号无权限，并建议联系文档所有者开通权限、确认分享链接权限，或切换到有权限的账号 |

---

## 工具组合速查

| 用户需求 | 推荐工具组合 |
|----------|-------------|
| 用户需要创建信息收集表单（报名、登记、问卷等），希望获得可分享的填写链接 | AI 设计题目 → `form.lite.create_draft` → `form.lite.publish_draft` → 返回填写链接 |
