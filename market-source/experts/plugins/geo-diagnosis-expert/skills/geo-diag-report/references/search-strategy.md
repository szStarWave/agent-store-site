# GEO诊断报告 - 搜索增强策略与执行规范

---

## 双模式架构

| 模式 | 触发条件 | 数据来源 | 质量 |
|------|----------|----------|------|
| 搜索增强（默认） | WebSearch 可用 | 真实搜索结果 + AI推理填充 | 高 |
| 纯虚拟（自动降级） | WebSearch 不可用 | 完全AI推理模拟 | 中 |

**降级链**：WebSearch → AI推理

各阶段执行时自动检测工具可用性，优先使用搜索增强，搜索失败或结果不足时自动降级为AI推理，无需用户干预。

---

## 并行搜索策略（v4.0优化）

> 核心优化：将串行搜索改为分组并行，减少网络等待时间。

### 阶段1 搜索组

| 组别 | 搜索词 | 并行方式 |
|------|--------|----------|
| **组A（并行）** | 品牌官网+品牌介绍、竞品信息、评测报道 | 3次WebSearch同时发起 |
| **组B（并行）** | 自媒体site搜索、权威媒体site搜索 | 2次WebSearch同时发起 |

组A和组B可串行（组A先执行，结果供组B参考调整），也可并行（若无需动态调整查询词）。

### 阶段2 搜索组

| 组别 | 搜索词 | 并行方式 |
|------|--------|----------|
| **组A（并行）** | "{brandName} {productType} 推荐"、"{brandName} {productType} AI搜索" | 2次WebSearch同时发起 |

### 阶段3 搜索组

| 组别 | 搜索词 | 并行方式 |
|------|--------|----------|
| **组A（并行）** | 负面投诉、口碑评价、风险争议 | 3次WebSearch同时发起 |

### 跨阶段并行

阶段2和阶段3**互不依赖**，可同时开始执行：
- 阶段2的搜索 + 阶段3的搜索 → 同时发起
- 阶段2的推理 + 阶段3的推理 → 同时执行

---

## 搜索缓存策略（v4.0优化）

> 对同一品牌+产品的重复诊断，缓存搜索结果避免重复请求。

### 缓存规则

| 缓存键 | 格式 | 过期时间 | 适用场景 |
|--------|------|----------|----------|
| 基建缓存 | `search-cache/{brand}-{product}-infra.json` | 30分钟 | 官网、媒体报道（变化慢） |
| 竞品缓存 | `search-cache/{brand}-{product}-competitor.json` | 1小时 | 竞品信息（变化很慢） |
| 舆情缓存 | `search-cache/{brand}-{product}-sentiment.json` | 15分钟 | 舆情数据（变化快） |

### 缓存命中判断

1. 检查 `diag-output/search-cache/` 下是否存在对应缓存文件
2. 读取缓存文件的 `cachedAt` 时间戳
3. 若未过期，直接使用缓存结果，跳过搜索
4. 若已过期或不存在，执行搜索并写入缓存

### 缓存文件格式

```json
{
  "cachedAt": "2026-06-10T10:30:00+08:00",
  "brand": "华为",
  "product": "汽车",
  "type": "infra",
  "results": [
    {"query": "华为汽车 品牌介绍", "items": [...]}
  ]
}
```

---

## 各阶段搜索增强点

| 阶段 | 搜索目标 | 搜索方式 | 降级策略 |
|------|----------|----------|----------|
| INFRA_EVAL step2 | 自媒体报道URL/标题 | WebSearch: 品牌词+平台site搜索 | AI推理生成（标注⚠️虚拟） |
| INFRA_EVAL step3 | 权威媒体报道URL/标题 | WebSearch: 品牌词+媒体site搜索 | AI推理生成（标注⚠️虚拟） |
| COMPETITOR | 竞品市场信息 | WebSearch: 品类排名/推荐 | AI推理填充 |
| SENTIMENT step1 | 舆情查询词 | 无需搜索（AI生成即可） | — |
| 虚拟收录查询 | 各平台回答模拟 | AI推理模拟（核心功能） | — |
| OVERVIEW/AIVO/SUGGESTION | 无需搜索（汇总R1+R2） | — | — |

---

## 搜索增强优先级

1. **有URL要求**的阶段优先使用搜索增强（INFRA_EVAL step2/step3）
2. **有事实性要求**的阶段次优先（COMPETITOR 市场份额、竞品信息）
3. **纯推理类**阶段无需搜索（OVERVIEW、AIVO_SCORE、虚拟收录查询）

### 搜索策略

始终使用搜索增强模式：关键阶段优先WebSearch获取真实数据，搜索失败或结果不足时降级为AI推理。无需用户选择，自动执行最佳方案。

---

## 速率控制

- 同一搜索引擎：请求间隔 1-2 秒
- **并行搜索：同组搜索同时发起，不设间隔（v4.0优化）**
- 批量搜索：组间间隔 1 秒（v4.0优化，原2秒）

---

## 搜索引擎选择

| 查询语言 | 首选引擎 | 备选引擎 |
|----------|----------|----------|
| 中文 | Baidu, Bing CN, Sogou | WeChat搜索, 360, Shenma |
| 英文 | Google, Bing INT, DuckDuckGo | Brave, Startpage |

---

## 重试与容错

| 异常 | 处理 |
|------|------|
| 搜索超时/无结果 | 重试1次，换引擎 |
| 搜索结果不相关 | 扩大/调整查询词，重试1次 |
| WebSearch 完全不可用 | 降级为纯虚拟模式 |
| 返回403/429 | 如有cookie机制则刷新后重试1次 |

---

## 结果验证

- URL格式校验：必须以 http(s):// 开头
- 内容相关性：搜索结果标题/摘要需包含品牌词或产品词
- 去重：同一URL只保留一条
- 时效性：优先展示近1年内内容

---

## 数据标注

搜索获取的数据 vs AI推理生成的数据，在报告中透明标注：

| 来源 | 标注 | 说明 |
|------|------|------|
| 真实搜索 | 无特殊标注 | 可信度高 |
| AI推理 | ⚠️虚拟 | 未经验证的推理数据 |
| 混合 | 🔍增强 | 搜索+推理结合 |