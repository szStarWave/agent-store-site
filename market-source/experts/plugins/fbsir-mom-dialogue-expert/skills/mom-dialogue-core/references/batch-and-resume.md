# 批处理与续接

批量资料先按类型、大小、时间和来源稳定排序，先抽样校准，再分批执行。每批写入 CPK 检查点，至少包含：批次编号、已处理 `SRC`、处理能力、参数、产出文件、待处理项、待确认项和锁定章节。

续接先运行 `diff_sources.py`，只重跑 `added`、`modified` 和明确标记 `needs_reanalysis=yes` 的来源；`unchanged` 不重复分析；`missing` 进入待核对清单，不自动删除记录。跨批人物使用同一 `PER` 档案，无法确认时保持候选状态。
