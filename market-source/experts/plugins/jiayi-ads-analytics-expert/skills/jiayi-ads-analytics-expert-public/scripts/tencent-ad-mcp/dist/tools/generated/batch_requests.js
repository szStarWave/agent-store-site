// Auto-generated from Go SDK — module: batch_requests
import { z } from "zod/v4";
import { apiPost } from "../../client.js";
const jsonText = (data) => ({
    content: [{ type: "text", text: JSON.stringify(data, null, 2) }],
});
export function registerBatchRequestsTools(server) {
    server.tool("batch_requests_add", "创建批量请求", {
        account_id: z.number().optional().describe("广告主账户ID"),
        params: z.record(z.string(), z.unknown()).optional().describe("其他请求参数 (key-value)"),
    }, async ({ account_id, params }) => {
        const merged = { ...params, ...(account_id != null ? { account_id } : {}) };
        return jsonText(await apiPost("/batch_requests/add", merged));
    });
}
//# sourceMappingURL=batch_requests.js.map