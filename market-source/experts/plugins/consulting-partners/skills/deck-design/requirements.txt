# deck-design 依赖
# 核心(生成 PPT 必需):
python-pptx>=1.0.0
lxml>=4.9.0

# 可选(仅 AI 自动生成封面图 cover_image='auto' 时需要;不装则用纯色/几何封面,不影响主流程):
# Pillow>=9.0
# numpy>=1.24
# rembg>=2.0
# tencentcloud-sdk-python>=3.0
