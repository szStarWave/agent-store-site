interface:
  display_name: "简历 AI 帮你写"
  short_description: "通过追问优化简历字段并生成本地 HTML 简历"
  default_prompt: "Use $resume-ai-help to optimize my resume and generate the final local HTML resume."

policy:
  allow_implicit_invocation: true
