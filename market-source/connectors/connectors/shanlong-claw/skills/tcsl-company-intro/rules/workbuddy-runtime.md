# WorkBuddy 运行兼容性（本 Skill 强制）

> 与 `SKILL.md` 中「WorkBuddy 运行兼容」章节一致；冲突时以本文件 + 隐私边界为准。

## 执行边界

本 Skill 只做天财商龙公司介绍。不执行经营/会员/供应链查询，也不引导企业版安装、专家选择或技能路由。

禁止：`--verbose`/`-v`、`--header`、`--envPath`、`--body-file`、`<sl-sea-absolute> token show`、动态 DataCube、可变任务 ID、`title/where`、直连 MCP/DB、探索性 help/扫命令。

## 隐私

不向对话输出 Token/密钥/完整认证参数；不展示可绕过权限的内部实现细节。默认不做触达/发券/导出明细/改库存或成本卡等写操作。
