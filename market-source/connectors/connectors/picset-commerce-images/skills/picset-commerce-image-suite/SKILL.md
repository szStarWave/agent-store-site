---
name: picset-commerce-image-suite
description: "根据用户提供商品图一键生成电商主图、详情图、套图、Listing 图和 A+ 商品图，支持卖点图、场景图、功能图等上架素材。覆盖淘宝、天猫、京东、拼多多、抖音、1688、小红书、TikTok、Amazon、Shopify、Temu、OZON、Shopee、阿里巴巴国际站等主流平台，可自定义比例、分辨率和数量，支持按编号局部修改。"
---

# Picset 电商套图

通过简洁的对话生成电商主图、详情图、套图、Listing 图和 A+ 商品图。默认使用简体中文。

## 职责与边界

负责把用户的商品图和自然语言要求整理成一份可修改的方案，规划每张图片的商业任务，完成报价确认，并按 [共享执行手册](../shared/execution-playbook.md) 完成上传、登记、生成、轮询和交付。

只处理商品主图、详情图、套图、Listing 图和 A+ 商品图。**哪怕用户只要一张主图或一张详情图，也必须走本套图链路，不得路由到单图文生图/图生图。** 闲聊/创意单图、图片编辑交给 [单图文生图/图生图 Skill](../picset-single-image-generation/SKILL.md)；打开画布、充值套餐交给 [Agent Canvas Skill](../picset-agent-canvas/SKILL.md)；套图交付后写回画布按 [Agent Canvas 共享手册](../shared/agent-canvas-playbook.md)。闲聊单图不得调用 `generate_commerce_images`。

开始前完整读取 [公共交接协议](../shared/handoff-protocol.md)。如果被直接触发且没有收到上下文，按协议初始化空上下文，不创建第二套字段。

## 推荐方案

| 场景 | 推荐配置 |
| --- | --- |
| "做一套电商图" | 5 张主图 + 6 张详情图 |
| "主图套图" | 5 张主图 |
| "详情图套图" | 6 张详情图 |
| "Listing 图" | 等同于主图套图，按平台规范生成 |
| 单张主图 | `M1` |
| 单张详情图 | `D1` |

通用推荐值：平台=天猫，语言=中文，主图比例=1:1，详情图比例=3:4，分辨率=2K。

用户指定平台但未指定市场和语言时，能确定默认值的直接推荐（如国内平台推荐中国中文、Amazon 推荐美国英语、OZON 推荐俄罗斯俄语），无法确定的询问用户，不强行给默认值。

用户指定数量、平台、市场、语言、比例或分辨率时优先采用用户提供的内容。

**推荐配置展示**：收到商品图并完成识别后，在展示套图规划之前，必须先用表格形式展示推荐配置，不得用纯文字一行带过。

表格格式：

| 配置项 | 推荐值 |
| --- | --- |
| 平台 | 天猫（中文） |
| 主图 | 5 张，比例 1:1，分辨率 2K |
| 详情图 | 6 张，比例 3:4，分辨率 2K |

表格后紧接套图规划内容。用户指定了平台、数量、比例或分辨率时，表格中使用用户指定的值，不强行覆盖为推荐值。

## 完整可选值

只有用户询问"有哪些选项""可以设置什么"或回复"查看选项"时，才展示以下内容。

**平台（14 个）**：淘宝、天猫、京东、拼多多、抖音、1688、小红书、TikTok、Amazon、Shopify、Temu、OZON、Shopee、阿里巴巴国际站。

**语言**：无文字、中文、英语、日语、韩语、西班牙语、葡萄牙语、俄语、德语、法语、泰语、印尼语、阿拉伯语、意大利语、越南语、马来语、罗马尼亚语。

**图片比例**：1:1、2:3、3:2、3:4、4:3、4:5、5:4、9:16、16:9、21:9。主图默认 1:1，详情图默认 3:4，用户可指定其他比例。

**分辨率**：1K、2K、4K。默认 2K。

**图片类型与数量**：
- 主图（M）：1-16 张
- 详情图（D）：1-16 张
- 套图：主图 + 详情图组合，总数 2-32 张
- Listing 图：按主图处理，1-16 张
- A+ 商品图：按详情图处理，1-16 张

列出选项后提醒用户无需逐项填写，可以直接说：`做 TikTok 东南亚站印尼语，3 张主图加 4 张详情图，比例 9:16，分辨率 4K。`

## 渐进式首轮

先判断用户意图，不得把几种场景合并回答。

**直接生成**：用户说"帮我做电商图""生成商品套图"但尚未上传商品图时，回复：

> 可以，直接上传商品图即可开始。
> 推荐生成 5 张主图 + 6 张详情图，天猫中文，主图 1:1、详情图 3:4，分辨率 2K。
> 想调整可回复"查看选项"；也可以直接说"做 Amazon 美国英文 3 张主图加 4 张详情图"。

不要在这一步展开完整参数，不追问平台、市场或语言。

**询问完整选项**：只有用户明确询问"有哪些选项"时，才展示上面"完整可选值"章节的内容，并区分必须提供、可以调整和默认内容。

## 套图规划

如果用户有明确要求，根据用户要求，如果用户没有要求根据商品实际情况进行任务规划调整，下面是示例，不是固定模板。

主图任务示例：
- `M1`：商品识别与首屏吸引
- `M2`：第一核心卖点
- `M3`：第二核心卖点或结构细节
- `M4`：使用场景或目标用户
- `M5`：规格、包装、组合内容或信任信息

详情图示例：
- `D1`：商品价值总览
- `D2`：用户痛点与解决方式
- `D3`：核心功能拆解
- `D4`：使用步骤或真实场景
- `D5`：尺寸、规格、兼容性或对比
- `D6`：包装清单、品牌信任或收尾信息

这些是规划起点，根据可靠商品事实重新编排，避免重复表达。用户指定数量时按比例分配主图和详情图的任务，不足时合并、超出时拆分。

**商品事实安全**：只使用用户明确提供或图片可见的信息。无法可靠确认的材质、数值性能、认证、兼容性、医疗效果、销量排名等，放入不确定事实并追问可核对资料，不得编造为卖点或文案。

## 执行流程

方案确认后，按以下顺序执行。具体操作见 [共享执行手册](../shared/execution-playbook.md)。

1. **只读报价**：调用 `quote_commerce_image_credits`，展示主图/详情图的数量、比例、分辨率、单价、小计和总计。该回合不上传、不登记、不生成。
2. **积分确认**：先读取用户偏好，若设置为跳过积分确认则直接继续；否则向用户明确询问是否接受预估积分，说明最终按提交时实时积分扣除，可能与预估不同，等待用户新的明确确认消息。
3. **上传登记**：用户确认积分后，对每个本地素材执行 `get_reference_image_upload_token` → `picset_client.py upload` → `register_reference_image`。
4. **提交生成**：素材全部登记后，按主图/详情图/A+ 商品图分批调用 `generate_commerce_images`，保存返回的 `task_id`。
5. **静默轮询**：固定 30 秒间隔调用 `get_generation_task_status`，直到所有批次进入终态。
6. **结果交付**：按稳定编号恢复结果，将成功图片下载到本地，通过 `present_files` 展示本地路径。具体操作见 [共享执行手册](../shared/execution-playbook.md) 第六节。

不探索其他脚本、工具或服务。任一阶段失败时，保留已完成状态，不自动重建已提交任务。

## 局部返工

用户点名 `M3`、`D2` 等已生成图片时，只修改该编号，其他图片、稳定编号和视觉系统保持不变。

> 每次只处理你点名的这一张，其他图片保持不变。

- **重做**：只重新生成这一张，不附加修改要求。
- **改画面**：将用户原话放入修改要求，不替用户扩写未提出的改动。
- **改文字**：整理为原文字和新文字的对应关系，一次可替换一处或多处。

局部返工不必须经过新的积分确认。全局平台、数量、语言或视觉系统变化时，回到草稿确认阶段。

## 结果展示

全部成功时，最终回复遵循以下格式（编号、数量、积分按真实结果替换）：

> 已生成：M1–M5、D1–D6（11/11 成功）
> 本次消耗：42 积分

部分成功时：

> 已生成：M1–M5（5/5 成功）、D1–D4（4/6 成功）
> D5、D6 生成失败，可单独重试。
> 本次消耗：35 积分

全部失败时如实说明，不展示或编造结果，提供重试入口。

生成结果下载到本地后通过 `present_files` 展示，不在对话中重复输出图片链接或 Markdown 图片节点。

## MCP 工具参数速查

详细参数和返回值说明见 [共享执行手册](../shared/execution-playbook.md)。以下为快速索引：

| 工具 | 用途 | 调用阶段 |
| --- | --- | --- |
| `get_reference_image_upload_token` | 获取短期 OSS 上传凭证 | 上传登记 |
| `register_reference_image` | 登记已上传素材为参考图 URL | 上传登记 |
| `quote_commerce_image_credits` | 只读报价，返回预估积分 | 报价 |
| `generate_commerce_images` | 提交生成任务，返回 task_id | 生成提交 |
| `get_generation_task_status` | 查询任务状态和结果 | 轮询 |

调用 MCP 工具前，必须使用 `tool_search` 按工具名获取完整参数定义，再按取回的定义发起调用。

套图经 `present_files` 交付后若需同步到画布，按 [Agent Canvas 共享手册](../shared/agent-canvas-playbook.md) 的写回章节执行。
