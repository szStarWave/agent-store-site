# createToBOrder - 创建B端订单

本文档覆盖创建B端订货单接口

## 接口目录
- [createToBOrder](#createtoborder) - 创建B端订单

---

## createToBOrder

**功能**：创建一个B端订货单（B2B订单），支持卖方为客户代下单。下单前必须先调用 `calculateOrderPrice` 获取计价结果，金额字段需严格使用计价返回值。

**典型场景**
- 卖方为客户创建一笔B2B订单
- 创建订单时同步出库/发货/签收（POS场景）
- 创建订单时同步记录线下付款信息

**注意事项**
> **非幂等接口**：调用时保留调用结果, 用于提取关键信息(如订单号、订单ID等)。禁止因获取【结果】而重复调用此接口。

**参数**

| 参数 | 类型 | 必填 | 数组 | 说明 |
|------|------|------|------|------|
| buyerId | NUMBER | 否 | 否 | 买方机构ID，不传默认当前登录机构 |
| sellerId | NUMBER | 否 | 否 | 卖方机构ID，不传默认当前登录机构 |
| productTotalAmount | NUMBER | 是 | 否 | 商品小计总额（每行商品小计之和，必须与计价结果一致） |
| productDiscountAmount | NUMBER | 是 | 否 | 商品优惠总额（优惠券和积分之外的折扣/促销优惠金额） |
| orderDiscountAmount | NUMBER | 是 | 否 | 整单优惠金额（整单改价的优惠，不含优惠券） |
| totalFreight | NUMBER | 是 | 否 | 运费（必须与计价结果一致） |
| couponAmount | NUMBER | 是 | 否 | 优惠券金额 |
| orderTotalAmount | NUMBER | 是 | 否 | 结算总额（= productTotalAmount + totalFreight - orderDiscountAmount） |
| orderDetails | ARRAY | 是 | 是 | 购买商品列表（包含赠品），最多100行 |
| addressId | NUMBER | 否 | 否 | 收货地址ID，从 `getCustomerDetail.deliveryAddresses` 获取 |
| contactName | STRING | 否 | 否 | 收货联系人（最多32字符） |
| contactPhone | STRING | 否 | 否 | 联系电话（最多32字符） |
| countryId | NUMBER | 否 | 否 | 收货地址-国家级区域ID，送货上门时传 `1`（中国） |
| provinceId | NUMBER | 否 | 否 | 收货地址-省级行政区域ID |
| cityId | NUMBER | 否 | 否 | 收货地址-地级市区域ID |
| districtId | NUMBER | 否 | 否 | 收货地址-县级区域ID |
| addressDetail | STRING | 否 | 否 | 收货地址-详细地址（最多200字符） |
| addressLongitude | STRING | 否 | 否 | 收货地址的经度 |
| addressLatitude | STRING | 否 | 否 | 收货地址的纬度 |
| deliveryMethodId | NUMBER | 否 | 否 | 配送方式ID |
| deliveryDate | DATETIME | 否 | 否 | 交货日期，格式 `yyyy-MM-dd HH:mm:ss` |
| warehouseId | NUMBER | 否 | 否 | 发货仓库ID |
| remark | STRING | 否 | 否 | 订单备注（最多256字符） |
| salesmanId | NUMBER | 否 | 否 | 业务员ID，不传使用客户默认业务员 |
| createId | NUMBER | 否 | 否 | 制单人员工ID |
| couponId | NUMBER | 否 | 否 | 优惠券ID |
| customFreight | BOOLEAN | 否 | 否 | 是否自定义运费，默认 `false`（false时按系统运费模板） |
| bargainFlag | BOOLEAN | 否 | 否 | 是否特价单 |
| ignorePromotion | BOOLEAN | 否 | 否 | 是否忽略促销，默认 `false` |
| genInvisibleOrder | BOOLEAN | 否 | 否 | 是否生成不可见订单（未支付时不可见），默认 `false` |
| point | NUMBER | 否 | 否 | 本次订单使用的积分数量 |
| pointAmount | NUMBER | 否 | 否 | 积分抵扣金额 |
| rebateAmount | NUMBER | 否 | 否 | 返利金额 |
| orderRebateInfos | ARRAY | 否 | 是 | 使用返利账户信息列表 |
| cartItemIds | NUMBER | 否 | 是 | 购物车记录ID列表（下单后删除购物车内容） |
| orderCategory | STRING | 否 | 否 | 订单类别 |
| buyerDepartmentId | NUMBER | 否 | 否 | 买方部门ID |
| buyerDepartmentName | STRING | 否 | 否 | 买方部门名称 |

**订单明细 orderDetails[ ]**

| 参数 | 类型 | 必填 | 数组 | 说明 |
|------|------|------|------|------|
| orderDetails[].productCode | STRING | 否 | 否 | 商品编码 |
| orderDetails[].productName | STRING | 否 | 否 | 商品名称 |
| orderDetails[].productSku | NUMBER | 否 | 否 | 商品SKU ID（来自 `findOrderCustomerSku.id`） |
| orderDetails[].unitId | NUMBER | 否 | 否 | 下单单位ID（多单位取大单位id，来自 `multiUnitList[].id`） |
| orderDetails[].purchaseNumbers | NUMBER | 否 | 否 | 购买数量 |
| orderDetails[].auxUnitId | NUMBER | 否 | 否 | 辅助计量单位ID |
| orderDetails[].auxUnitNumbers | NUMBER | 否 | 否 | 辅助单位购买数量 |
| orderDetails[].originalPrice | NUMBER | 否 | 否 | 商品原价（含改价） |
| orderDetails[].salePrice | NUMBER | 否 | 否 | 折后单价（来自计价结果） |
| orderDetails[].subtotal | NUMBER | 否 | 否 | 小计（purchaseNumbers × salePrice，不含优惠券） |
| orderDetails[].saleRate | NUMBER | 否 | 否 | 折扣率（0~100） |
| orderDetails[].customPrice | BOOLEAN | 否 | 否 | 是否自定义改价（true时不参与促销） |
| orderDetails[].type | STRING | 否 | 否 | 商品类型（NORMAL=普通商品 MANUAL_GIFT=手动赠品 PROMOTION_GIFT=促销赠品） |
| orderDetails[].activityId | NUMBER | 否 | 否 | 所属促销活动ID（促销赠品必填） |
| orderDetails[].remark | STRING | 否 | 否 | 行备注 |
| orderDetails[].warehouseId | NUMBER | 否 | 否 | 下单时商品仓库ID |

**订单发票 orderInvoice**

| 参数 | 类型 | 必填 | 数组 | 说明 |
|------|------|------|------|------|
| orderInvoice.invoiceType | STRING | 否 | 否 | 发票类型（NONE/普通发票/专票等），默认 `NONE` |
| orderInvoice.taxInfoId | NUMBER | 否 | 否 | 发票抬头信息记录ID |
| orderInvoice.taxId | STRING | 否 | 否 | 纳税人识别号 |
| orderInvoice.invoiceTitle | STRING | 否 | 否 | 发票抬头 |
| orderInvoice.address | STRING | 否 | 否 | 联系地址 |
| orderInvoice.phone | STRING | 否 | 否 | 联系电话 |
| orderInvoice.bankName | STRING | 否 | 否 | 开户行 |
| orderInvoice.bankAccount | STRING | 否 | 否 | 银行账号 |
| orderInvoice.email | STRING | 否 | 否 | 接收发票邮箱 |

**订单支付信息 paymentInfo[ ]（不支付设置null）**

| 参数 | 类型 | 必填 | 数组 | 说明 |
|------|------|------|------|------|
| paymentInfo[].paymentMethod | STRING | 否 | 否 | 支付方式（CASH=现金 BANK_TRANSFER=银行转账） |
| paymentInfo[].receiptAccountId | NUMBER | 否 | 否 | 收款账号ID（银行转账时必填） |
| paymentInfo[].payAmount | NUMBER | 否 | 否 | 支付金额 |

**响应数据**
- 返回类型：`OBJECT`

| 字段 | 类型 | 说明 |
|------|------|------|
| id | NUMBER | 订单ID |
| orderCode | STRING | 订单号 |
| orderStatus | STRING | 订单状态 |
| paymentStatus | STRING | 付款状态 |
| orderTotalAmount | NUMBER | 结算总额（订单金额） |
| productTotalAmount | NUMBER | 商品小计总额 |
| totalFreight | NUMBER | 运费 |
| couponAmount | NUMBER | 优惠券金额 |
| pointAmount | NUMBER | 其它费用 |
| otherAmount | NUMBER | 积分抵扣金额 |
| createTime | DATETIME | 创建时间 |
| buyer | OBJECT | 买方机构信息 |
| seller | OBJECT | 卖方机构信息 |
| settlementOrg | OBJECT | 结算机构信息 |
| salesmanId | NUMBER | 销售员ID |
| salesmanName | STRING | 销售员名字 |
| salesPhone | NUMBER | 销售员手机号码 |
| deliveryAddress | STRING | 收货地址 |
| contactName | STRING | 收货联系人 |
| contactPhone | STRING | 联系电话 |

**注意**

1. **必填字段**：`productTotalAmount`、`productDiscountAmount`、`orderDiscountAmount`、`totalFreight`、`couponAmount`、`orderTotalAmount`、`orderDetails` 为必填
2. **金额一致性**：所有金额字段必须与 `calculateOrderPrice` 的计价结果完全一致，否则会报错
   - `orderTotalAmount` = `productTotalAmount + totalFreight - orderDiscountAmount`
3. **countryId 必传**：送货上门场景下，`countryId` 即使不在必填列表中也必须传 `1`（中国），否则报"获取数据异常"
4. **促销场景**：当促销覆盖全部商品时，`productTotalAmount` 应传 `0`，对应商品行的 `salePrice` 和 `subtotal` 也传 `0`
5. **warehouseId 默认传 -1**：代表"全部仓库"，不传具体仓库ID
6. **orderDetails[].type**：必须是字符串枚举值：
   - `"NORMAL"`：普通商品
   - `"MANUAL_GIFT"`：手动赠品
   - `"PROMOTION_GIFT"`：促销赠品（需传 `activityId`）
7. **商品行数限制**：最多 100 行，超出需告知用户
8. **支付**：`paymentInfo` 为POS场景预留，普通下单设为 `null` 即可