---
name: wind-finance
description: 通过自然语言查询股票、基金、指数、债券、公告、财经新闻和宏观经济数据。
description_zh: 通过自然语言查询股票、基金、指数、债券、公告、财经新闻和宏观经济数据。
description_en: Query stocks, funds, indices, bonds, announcements, financial news, and macroeconomic data through natural language.
version: "1.2.0"
author: Wind
---

# Wind Alice 万得金融数据工具指南

# 总述：何时使用 Wind Alice 万得金融数据 MCP

当用户需要**可追溯的证券、基金、指数、债券、公告、财经新闻或宏观经济结构化数据**时，使用本 Connector。它适合获取事实、行情、指标、筛选结果和时间序列；不要把它当作通用投资建议工具，也不要在未调用工具时编造实时数据或历史数据。

## 路由与调用原则

1. **先识别对象。** 股票、基金、指数、债券、公司公告/新闻、宏观经济数据分别使用对应分类工具；若用户没有给出具体证券、基金或指数，只描述筛选条件，先用 `search_stocks` 或 `search_funds` 取得候选代码。
2. **再匹配时间粒度。** 分钟级日内序列用 `*_quote`；多日 OHLC/K 线用 `*_kline`；只要最新快照或指定指标点用 `*_price_indicators`；技术指标、风险指标、基本面指标分别用 `*_technicals`、`get_risk_metrics`、`*_fundamentals`。
3. **股票主题按意图路由。** 公司身份、上市与行业用 `get_stock_basicinfo`；股本、股东和限售解禁用 `get_stock_equity_holders`；报表、盈利和估值用 `get_stock_fundamentals`；公司重大事项、违规和分红用 `get_stock_events`。
4. **区分信息来源。** 查询上市公司披露文件、定期报告或公告正文时用 `get_company_announcements`；查询媒体报道、市场动态和新闻时用 `get_financial_news`。
5. **宏观数据显式指定 EDB 模式。** 找指标用“仅搜索”；已知 EDB 代码取数用“仅提数”；自然语言找指标并返回数值用“搜索并提数”。取数时传 `beginDate`/`endDate`，或传 `observation`，两者不可同时传。
6. **多标的汇总或计算用 `get_financial_data`。** 将需要的实体、指标、日期、筛选或计算口径写清楚。工具返回空数据或错误时，说明结果并收窄对象、指标或时间范围后再尝试。

# 股票工具

## `search_stocks`

根据自然语言筛选条件从 A 股及海外股票市场反查股票，返回符合条件的股票代码列表。支持按指标数值（行情、财务、估值、资金、技术等）与业务分类（行业板块、主题概念、主营业务）组合条件，返回的代码可传入各属性查询工具获取具体数据。已指定具体股票时不要调用本工具。

> **适用场景：** 当用户没有给出具体股票代码，而是描述市值、涨跌幅、行业、上市板、连续上涨、资金流入等选股条件时使用。适合先生成候选 A 股及海外股票代码列表，再继续调用基本面、行情或事件类工具做深入分析。

> **适合的问法：** “帮我找出沪深市场市值超过500亿、最近5日上涨的股票。”

**输入参数**

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | :---: | --- |
| question | string | 是 | 自然语言选股问句，描述筛选条件（如市值、涨跌幅、行业、上市板等），返回符合条件的代码列表。例：筛选沪深市场市值超500亿且连续5日上涨的股票 |

**返回内容**

根据自然语言筛选条件从 A 股及海外股票市场反查股票，返回符合条件的股票代码列表。

**调用示例**

```json
{
  "name": "search_stocks",
  "arguments": {
    "question": "筛选沪深市场市值超500亿且近5日上涨的A股"
  }
}
```

---

## `get_stock_basicinfo`

返回指定股票的公司身份与分类档案，为静态数据，包括：简称、代码、曾用名、上市板块与上市日期、行业分类、概念标签、指数成份归属、工商注册信息、主营业务简介，以及当前状态标签（是否 ST、是否已摘牌）。仅返回当前状态，不含状态变动历史、财务数据与股东明细。

> **适用场景：** 查询公司身份、上市和行业档案。

**输入参数**

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | :---: | --- |
| question | string | 是 | 自然语言查询要求：股票实体（名称或股票代码）+ 需要的档案项（简称、上市板、上市日期、WIND行业分类、所属概念板块、经营范围、主营产品、是否风险警示等）。静态或准静态档案，通常无需时间限定。示例：查询贵州茅台的上市板与行业分类。 |

**返回内容**

返回指定股票的公司身份与分类档案，为静态数据，包括：简称、代码、曾用名、上市板块与上市日期、行业分类、概念标签、指数成份归属、工商注册信息、主营业务简介，以及当前状态标签（是否 ST、是否已摘牌）。

**调用示例**

```json
{
  "name": "get_stock_basicinfo",
  "arguments": {
    "question": "查询贵州茅台股票的基本档案，包括上市日期、所属行业和主营业务"
  }
}
```

---

## `get_stock_equity_holders`

返回指定股票的股本结构与股东构成，为准静态数据，随报告期与权益变动更新，包括：总股本、流通 A 股、限售股、自由流通股本及占比；前十大股东与前十大流通股东持仓及变动；机构股东持股；实际控制人与大股东详情；限售解禁时间表与本期解禁数量。不含市值（由价格与股本派生）。

> **适用场景：** 查询股票股本、股东结构和限售解禁。

**输入参数**

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | :---: | --- |
| question | string | 是 | 自然语言查询要求：证券实体 + 股本/股东指标（总股本、流通A股、限售股、前十大股东、解禁时间表等）+ 报告期。示例：查询贵州茅台(600519.SH)的前十大股东及流通A股占比。 |

**返回内容**

返回指定股票的股本结构与股东构成，为准静态数据，随报告期与权益变动更新，包括：总股本、流通 A 股、限售股、自由流通股本及占比；前十大股东与前十大流通股东持仓及变动；机构股东持股；实际控制人与大股东详情；限售解禁时间表与本期解禁数量。

**调用示例**

```json
{
  "name": "get_stock_equity_holders",
  "arguments": {
    "question": "查询贵州茅台（600519.SH）的前十大股东及流通A股占比"
  }
}
```

---

## `get_stock_fundamentals`

返回指定股票的两类基本面数据。第一类为财务原始指标：三大报表科目，偿债、盈利、成长、杠杆等财务比率（如 ROE、毛利率、资产负债率、速动比率），以及行业专项指标（如银行净息差、不良贷款率），时间维度为报告期。第二类为衍生估值指标：PE、PB、PS（TTM 口径）、股息率、总市值与流通市值及其历史分位数，随交易日变动。不含价格序列。

> **适用场景：** 查询财务报表、盈利能力和估值。

**输入参数**

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | :---: | --- |
| question | string | 是 | 自然语言查询要求：证券实体（名称或Wind代码）+ 财务/估值指标 + 日期或报告期。示例：查询贵州茅台(600519.SH)2024-12-31的ROE、营业收入和净利润。 |

**返回内容**

返回指定股票的两类基本面数据。

**调用示例**

```json
{
  "name": "get_stock_fundamentals",
  "arguments": {
    "question": "查询贵州茅台（600519.SH）2024-12-31的ROE、营业收入和净利润"
  }
}
```

---

## `get_stock_quote`

返回指定股票的分钟级量价序列，每条记录代表一分钟，包含开盘价、最新成交价、最高价、最低价、成交量、成交额、均价与换手率。时间范围由 begin/end 指定（含首尾），未指定默认最新交易日；单日约 240 条，跨日体积按天数放大，长区间建议改用 K 线工具的聚合周期。仅交易日有数据，非交易日返回空结果。当前时刻截面状态请用行情快照工具。

> **适用场景：** 查询股票日内分钟级行情。

**输入参数**

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | :---: | --- |
| windcode | string | 是 | 股票名称或者股票代码，如贵州茅台或600519.SH。 |
| begin | string | 否 | 开始日期，格式 yyyy-MM-dd，如 2026-03-25；未指定默认最新交易日。不可只传 end 不传 begin。 |
| end | string | 否 | 结束日期，格式 yyyy-MM-dd，如 2026-03-25；未指定默认最新交易日（即只传 begin 时返回 begin 至最新交易日的区间）。 |
| count | integer | 否 | 在开始/结束日期区间内取数的条数(整数):正数从头往后取N条，负数从结束日期往前取N条，0取全部；不会超出日期区间，默认0 |

**返回内容**

返回指定股票的分钟级量价序列，每条记录代表一分钟，包含开盘价、最新成交价、最高价、最低价、成交量、成交额、均价与换手率。

**调用示例**

```json
{
  "name": "get_stock_quote",
  "arguments": {
    "begin": "2026-07-27",
    "end": "2026-07-27",
    "windcode": "600519.SH"
  }
}
```

---

## `get_stock_kline`

返回指定股票在给定时间范围内的聚合量价序列（K 线），聚合周期由 period 指定（分钟级至年，默认日 K）。每条记录代表一个周期，包含开盘价、收盘价、最高价、最低价、成交量、成交额、均价、换手率。当日盘中分钟走势建议用分钟级行情工具（缺省即最新交易日）；锚定当前的相对窗口统计（近 N 日涨跌幅、年初至今涨跌幅、52 周高低）与基准指数对比不在本工具范围。

> **适用场景：** 查询股票日/周/月等 K 线。

**输入参数**

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | :---: | --- |
| windcode | string | 是 | 股票名称或者股票代码，如贵州茅台或600519.SH。 |
| begin_date | string | 是 | 开始日期：格式yyyy-MM-dd，如2026-03-25. |
| end_date | string | 是 | 结束日期，格式为yyyy-MM-dd,如2026-03-26. |
| period | string | 否 | K线周期：默认10(日K)。可选值：1=1分钟，3=5分钟，4=10分钟，5=15分钟，6=30分钟，7=60分钟，8=120分钟，9=240分钟，10=日K，11=周K，12=月K，13=年K，14=季K，15=半年K。常用：日K=10，周K=11，月K=12 |
| count | integer | 否 | 在开始/结束日期区间内取数的条数(整数):正数从头往后取N条，负数从结束日期往前取N条，0取全部；不会超出日期区间，默认0 |
| aftype | string | 否 | 复权类型：0=前复权，1=后复权，2=不复权，默认0。 |
| issusp | string | 否 | 是否包含停牌数据：0=不包含，1=包含，默认1 |
| afdate | string | 否 | 复权基准日期，格式yyyy-MM-dd，如2026-03-25。通常不需要指定 |

**返回内容**

返回指定股票在给定时间范围内的聚合量价序列（K 线），聚合周期由 period 指定（分钟级至年，默认日 K）。

**调用示例**

```json
{
  "name": "get_stock_kline",
  "arguments": {
    "begin_date": "2026-07-20",
    "end_date": "2026-07-27",
    "windcode": "600519.SH",
    "period": "10",
    "aftype": "0",
    "issusp": "1"
  }
}
```

---

## `get_stock_price_indicators`

获取一个或多个股票当前时刻的行情指标快照值（时点数据、非时间序列）。指标经 indexes 指定，取值须逐字取自其可选值清单（如 最新成交价、今日开盘价、今日最高价、今日最低价、涨跌幅、成交量、换手率等）；不填 indexes 时默认返回：最新交易日、交易时间、最新成交价、前收盘价、今日开盘价、今日最高价、今日最低价、成交量。仅返回时点截面，不含当日走势与历史区间序列。用户询问股票的当前/最新价格或某个行情指标值时使用。

**输入参数**

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | :---: | --- |
| windcode | string | 是 | 一个或多个股票名称或股票代码，多个代码用英文逗号分隔，如600519.SH,600000.SH。 |
| indexes | string | 否 | 行情指标字段，多个字段用逗号分隔。可选值：最新交易日,交易时间,中文简称,最新成交价,前收盘价,今日开盘价,今日最高价,今日最低价,最新均价,涨跌,涨跌幅,成交量,成交额,交易状态,上市日期,近1分钟成交额,近3分钟成交额,近5分钟成交额,K线实体涨跌幅,K线实体涨跌额,换手率,量比,振幅,基于Wind算法的量比,现量,现额,买一价,买二价,买三价,买四价,买五价,卖一价,卖二价,卖三价,卖四价,卖五价,买一量,买二量,买三量,买四量,买五量,卖一量,卖二量,卖三量,卖四量,卖五量,外盘,内盘,成交笔数,委比,当日主力净流入额,当日主力净流入占比,近5日主力净流入额,近5日主力净流入占比,近5日主力净流入天数,近10日主力净流入额,近10日主力净流入占比,近10日主力净流入天数,近20日主力净流入额,近20日主力净流入占比,近20日主力净流入天数,近60日主力净流入额,近60日主力净流入占比,近60日主力净流入天数,主力挂单买入,主力挂单卖出,主力撤单买入,主力撤单卖出,该日机构资金净流入额,该日大户资金净流入额,该日中户资金净流入额,该日散户资金净流入额,连红天数,连续上涨天数,火箭发射,高台跳水,涨停封板,跌停封板,涨停开板,跌停开板,涨幅达到3%,跌幅达到3%,创20日新高,创20日新低,盘前最新价,盘前涨跌额,盘前涨跌幅,盘前成交额,盘前涨速,盘后最新价,盘后涨跌幅,集合竞价涨跌幅,指数平滑异同移动平均,DIF快线,随机指标K值,随机指标D值,随机指标J值,6周期相对强弱指标,12周期相对强弱指标,抛物线转向指标,布林中轨,布林上轨,布林下轨,5周期移动平均,10周期移动平均,20周期移动平均,60周期移动平均,120周期移动平均,250日均线,5日乖离率,36日乖离,14周期顺势指标,26周期能量指标,12周期心理线指标,近1分钟涨跌幅,近3分钟涨跌幅,MACD多头金叉信号,MACD空头死叉信号,5分钟涨跌幅,5日涨跌幅,10日涨跌幅,20日涨跌幅,60日涨跌幅,120日涨跌幅,250日涨跌幅,年初至今涨跌幅,上市以来涨跌幅,近3年涨跌幅,近5年涨跌幅,近10年涨跌幅,近20年涨跌幅,流通份额,发行价,总股本,市净率,市净率(LF),市盈率(TTM),市盈率(LYR),市盈率(预测),总市值1,流通市值,总市值2,52周最高,52周最低,股息率,涨停价,跌停价,成分股贡献点数,近5分钟贡献度 默认值："最新交易日,交易时间,最新成交价,前收盘价,今日开盘价,今日最高价,今日最低价,成交量"。 |

**返回内容**

返回一个或多个股票当前时刻的行情指标快照值（时点数据、非时间序列）。

**调用示例**

```json
{
  "name": "get_stock_price_indicators",
  "arguments": {
    "windcode": "600519.SH,300073.SZ",
    "indexes": "最新交易日,交易时间,最新成交价,成交量,涨跌幅"
  }
}
```

---

## `get_stock_technicals`

返回指定股票由行情数据计算的派生指标，日频为主。相对窗口统计：近 N 日、周、月、年及年初至今涨跌幅，52 周最高最低价，相对基准指数表现，均锚定当前滚动。技术指标：趋势类、成交量类、超买超卖类、波动类（如 MACD、KDJ、RSI、BOLL 等）。技术形态：连续涨跌、创新高或新低检测、突破反转、涨跌停与连板状态等。不含原始行情序列、实时快照与风险统计量。

> **适用场景：** 查询技术分析、两融和龙虎榜。

**输入参数**

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | :---: | --- |
| question | string | 是 | 自然语言查询要求：股票实体（名称或股票代码）+ 技术指标或形态（MACD、KDJ、RSI、BOLL、均线、区间涨跌幅、连板/新高等）+ 日期。示例：查询贵州茅台(600519.SH)最近20日的收盘价与MACD。 |

**返回内容**

返回指定股票由行情数据计算的派生指标，日频为主。

**调用示例**

```json
{
  "name": "get_stock_technicals",
  "arguments": {
    "question": "查询贵州茅台（600519.SH）最近20日的收盘价、成交量和MACD"
  }
}
```

---

## `get_risk_metrics`

获取股票风险与波动性指标，涵盖Beta（标准、剔除财务杠杆、剔除所得税率）、Jensen Alpha（定期及年化）、波动率（年化及非年化，覆盖最近100周/24个月/60个月）、夏普比率（定期及年化）、最大回撤及区间下跌天数、在险价值VaR（参数法与历史法）、财务安全比率（资产负债率、速动比率、流动比率、净资产负债率）、以及基准相关系数。

> **适用场景：** 查询量化风险指标。

**输入参数**

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | :---: | --- |
| question | string | 是 | 自然语言查询要求：股票实体（名称或股票代码） + 量化风险指标（Beta、年化波动率、最大回撤、夏普比率、VaR等）+ 回看窗口或日期。示例：查询宁德时代(300750.SZ)过去1年的Beta、年化波动率和最大回撤。 |

**返回内容**

返回股票风险与波动性指标，涵盖Beta（标准、剔除财务杠杆、剔除所得税率）、Jensen Alpha（定期及年化）、波动率（年化及非年化，覆盖最近100周/24个月/60个月）、夏普比率（定期及年化）、最大回撤及区间下跌天数、在险价值VaR（参数法与历史法）、财务安全比率（资产负债率、速动比率、流动比率、净资产负债率）、以及基准相关系数。

**调用示例**

```json
{
  "name": "get_risk_metrics",
  "arguments": {
    "question": "查询宁德时代（300750.SZ）过去1年的Beta、年化波动率和最大回撤"
  }
}
```

---

## `get_stock_events`

返回指定股票的公司行动与事件数据，为结构化记录而非公告文本。事件类型覆盖：首发上市与再融资（配股、增发、并购重组）；分红派息；股本与股东事件（大股东增减持、限售解禁）；治理与监管事件（风险警示与 ST 变动、违规处罚、司法诉讼）。仅返回结构化字段，不含公告与新闻文本原文，不含公司当前状态标签。

> **适用场景：** 查询资本运作和公司事件。

**输入参数**

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | :---: | --- |
| question | string | 是 | 自然语言查询要求：股票实体（名称或股票代码）+ 事件类型（分红派息、增发配股、并购重组、增减持、限售解禁、ST变动、诉讼处罚等）+ 日期或时间范围。示例：查询贵州茅台(600519.SH)的分红派息历史。 |

**返回内容**

返回指定股票的公司行动与事件数据，为结构化记录而非公告文本。

**调用示例**

```json
{
  "name": "get_stock_events",
  "arguments": {
    "question": "查询贵州茅台（600519.SH）的分红派息历史"
  }
}
```

---

# 基金工具

## `search_funds`

根据自然语言筛选条件从全市场基金反查产品，返回符合条件的基金代码列表。支持按指标数值（业绩、规模、费率、持仓特征等）与产品分类（投资类型、风格、指数跟踪、管理公司等）组合条件，返回的代码可传入各属性查询工具获取具体数据。已指定具体基金时不要调用本工具。

> **适用场景：** 用户描述筛选条件、未指定基金。

**输入参数**

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | :---: | --- |
| question | string | 是 | 自然语言基金筛选条件，由指标及其筛选参数（运算条件、阈值、时间范围等）组成，无需指定具体基金实体。每个筛选指标可各自带时间窗口。筛选股票型基金中近一年收益率超20%的产品。 |

**返回内容**

根据自然语言筛选条件从全市场基金反查产品，返回符合条件的基金代码列表。

**调用示例**

```json
{
  "name": "search_funds",
  "arguments": {
    "question": "筛选股票型基金中近一年收益率超20%的产品"
  }
}
```

---

## `get_fund_info`

返回指定基金的产品档案，为静态或准静态数据，包括：代码、简称、全称；投资类型与风格；业绩比较基准与风险等级；费率结构（如管理费、托管费、申购赎回费）；基金经理（现任与历任，如任职期限、管理规模）；生命周期记录（如成立、转型、清盘、更名）；运作状态（如申购赎回状态）；管理人与托管人；发行信息（如成立日期、发行规模）；指数跟踪信息（如跟踪指数、上市日期、封闭运作期）。不含规模与变动、业绩数据与持仓明细。

> **适用场景：** 查询基金基础档案。

**输入参数**

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | :---: | --- |
| question | string | 是 | 自然语言查询要求：基金实体（名称或基金代码）+ 档案项（投资类型与风格、业绩比较基准、费率、现任基金经理、管理人/托管人、成立与发行信息、跟踪指数等）。静态或准静态档案，通常无需时间限定。示例：查询易方达蓝筹精选的投资类型、管理费率和现任基金经理。 |

**返回内容**

返回指定基金的产品档案，为静态或准静态数据，包括：代码、简称、全称；投资类型与风格；业绩比较基准与风险等级；费率结构（如管理费、托管费、申购赎回费）；基金经理（现任与历任，如任职期限、管理规模）；生命周期记录（如成立、转型、清盘、更名）；运作状态（如申购赎回状态）；管理人与托管人；发行信息（如成立日期、发行规模）；指数跟踪信息（如跟踪指数、上市日期、封闭运作期）。

**调用示例**

```json
{
  "name": "get_fund_info",
  "arguments": {
    "question": "查询易方达蓝筹精选混合基金的基本档案、基金经理和业绩比较基准"
  }
}
```

---

## `get_fund_holdings`

返回指定基金的投资组合数据，随定期报告披露更新，包含两族。披露持仓族：资产类别构成（如股票、债券、存款占净值比例及期间变动）；重仓资产（如重仓股票、重仓债券、FOF 的重仓基金，及占流通股比例、持仓变动等指标）；行业配置（如申万、Wind、中信口径）。组合派生特征族：由持仓计算的组合层面指标（如持股集中度、组合估值、基金换手率等）。仅返回基金组合层面的数据，个券的详细属性需以对应代码查询相应实体的数据。

> **适用场景：** 查询基金资产和重仓配置。

**输入参数**

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | :---: | --- |
| question | string | 是 | 自然语言查询要求：基金实体（名称或基金代码）+ 持仓/配置指标（资产类别配置、重仓股、重仓债、FOF重仓基金、行业配置、持股集中度等）+ 报告期。示例：查询易方达蓝筹精选2024年年报的前十大重仓股。 |

**返回内容**

返回指定基金的投资组合数据，随定期报告披露更新，包含两族。

**调用示例**

```json
{
  "name": "get_fund_holdings",
  "arguments": {
    "question": "查询易方达蓝筹精选混合基金最近一期重仓股和行业配置"
  }
}
```

---

## `get_fund_holders`

返回指定基金的份额、规模与持有人结构，包括：最新规模（资产净值、份额总数）及期间变动；持有人结构（如个人与机构持有比例、持有人户数）；申购赎回情况（如报告期及单季度份额变动）。不含静态产品档案。

> **适用场景：** 查询持有人结构与资金流。

**输入参数**

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | :---: | --- |
| question | string | 是 | 自然语言查询要求：基金实体 + 份额/持有人指标（资产净值、份额总数、持有人结构、机构/个人占比、申购赎回等）+ 报告期。示例：查询易方达蓝筹精选(005827.OF)的最新规模和持有人结构。 |

**返回内容**

返回指定基金的份额、规模与持有人结构，包括：最新规模（资产净值、份额总数）及期间变动；持有人结构（如个人与机构持有比例、持有人户数）；申购赎回情况（如报告期及单季度份额变动）。

**调用示例**

```json
{
  "name": "get_fund_holders",
  "arguments": {
    "question": "查询易方达蓝筹精选混合基金的机构持有人比例和持有人户数"
  }
}
```

---

## `get_fund_quote`

返回指定场内交易基金（ETF/LOF）的分钟级价格序列，每条记录代表一分钟，包含价格、均价、成交量与换手率。时间范围由 begin/end 指定（含首尾），未指定默认最新交易日；单日约 240 条，跨日体积按天数放大，长区间建议改用 K 线工具的聚合周期。仅交易日有数据，非交易日返回空结果。场外基金无场内行情，其价格口径为净值（属业绩与评价数据）。

> **适用场景：** 查询 ETF/LOF 分钟行情。

**输入参数**

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | :---: | --- |
| windcode | string | 是 | 基金名称或者基金代码，如588200.SH。 |
| begin | string | 否 | 开始日期，格式 yyyy-MM-dd，如 2026-03-25；未指定默认最新交易日。不可只传 end 不传 begin。 |
| end | string | 否 | 结束日期，格式 yyyy-MM-dd，如 2026-03-25；未指定默认最新交易日（即只传 begin 时返回 begin 至最新交易日的区间）。 |
| count | integer | 否 | 在开始/结束日期区间内取数的条数(整数):正数从头往后取N条，负数从结束日期往前取N条，0取全部；不会超出日期区间，默认0 |

**返回内容**

返回指定场内交易基金（ETF/LOF）的分钟级价格序列，每条记录代表一分钟，包含价格、均价、成交量与换手率。

**调用示例**

```json
{
  "name": "get_fund_quote",
  "arguments": {
    "begin": "2026-07-27",
    "end": "2026-07-27",
    "windcode": "510300.SH"
  }
}
```

---

## `get_fund_kline`

返回指定场内交易基金（ETF/LOF）在给定时间范围内的聚合价格序列（K 线），聚合周期由 period 指定（分钟级至年，默认日 K）。每条记录代表一个周期，包含开盘价、收盘价、最高价、最低价、成交量、成交额、换手率与均价。当日盘中分钟走势建议用分钟级行情工具（缺省即最新交易日）；场外基金无场内行情，其价格口径为净值（属业绩与评价数据）。

> **适用场景：** 查询 ETF/LOF K 线。

**输入参数**

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | :---: | --- |
| windcode | string | 是 | 基金名称或者基金代码，如588200.SH。 |
| begin_date | string | 是 | 开始日期：格式yyyy-MM-dd，如2026-03-25. |
| end_date | string | 是 | 结束日期，格式为yyyy-MM-dd，如2026-03-25. |
| period | string | 否 | K线周期：默认10(日K)。可选值：1=1分钟，3=5分钟，4=10分钟，5=15分钟，6=30分钟，7=60分钟，8=120分钟，9=240分钟，10=日K，11=周K，12=月K，13=年K，14=季K，15=半年K。常用：日K=10，周K=11，月K=12 |
| count | integer | 否 | 在开始/结束日期区间内取数的条数(整数):正数从头往后取N条，负数从结束日期往前取N条，0取全部；不会超出日期区间，默认0 |
| aftype | string | 否 | 复权类型：0=前复权，1=后复权，2=不复权，默认0。 |
| issusp | string | 否 | 是否包含停牌数据：0=不包含，1=包含，默认1 |
| afdate | string | 否 | 复权基准日期，格式yyyy-MM-dd，如2026-03-25。通常不需要指定 |

**返回内容**

返回指定场内交易基金（ETF/LOF）在给定时间范围内的聚合价格序列（K 线），聚合周期由 period 指定（分钟级至年，默认日 K）。

**调用示例**

```json
{
  "name": "get_fund_kline",
  "arguments": {
    "begin_date": "2026-07-20",
    "end_date": "2026-07-27",
    "windcode": "510300.SH",
    "period": "10",
    "aftype": "0",
    "issusp": "1"
  }
}
```

---

## `get_fund_price_indicators`

获取一个或多个场内交易基金（ETF/LOF）当前时刻的行情指标快照值（时点数据、非时间序列）。指标经 indexes 指定，取值须逐字取自其可选值清单（如 最新成交价、今日开盘价、涨跌幅、成交额、换手率等）；不填 indexes 时默认返回：最新交易日、交易时间、最新成交价、前收盘价、今日开盘价、今日最高价、今日最低价、成交量。仅返回时点截面，不含过程序列；场外基金的净值查询不在本工具范围。用户询问场内基金的当前/最新价格或某个行情指标值时使用。

> **适用场景：** 查询场内基金当前指标。

**输入参数**

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | :---: | --- |
| windcode | string | 是 | 一个或多个基金名称或基金代码（场内 ETF/LOF），多个代码用英文逗号分隔，如510300.SH,588200.SH。 |
| indexes | string | 否 | 行情指标字段，多个字段用逗号分隔。可选值：最新交易日,交易时间,中文简称,最新成交价,前收盘价,今日开盘价,今日最高价,今日最低价,最新均价,涨跌,涨跌幅,成交量,成交额,交易状态,上市日期,近1分钟成交额,近3分钟成交额,近5分钟成交额,近7日平均成交额,换手率,量比,振幅,基于Wind算法的量比,现量,现额,买一价,买二价,买三价,买四价,买五价,卖一价,卖二价,卖三价,卖四价,卖五价,买一量,买二量,买三量,买四量,买五量,卖一量,卖二量,卖三量,卖四量,卖五量,外盘,内盘,成交笔数,委比,当日主力净流入额,当日主力净流入占比,近5日主力净流入额,近5日主力净流入占比,近5日主力净流入天数,近10日主力净流入额,近10日主力净流入占比,近10日主力净流入天数,近20日主力净流入额,近20日主力净流入占比,近20日主力净流入天数,近60日主力净流入额,近60日主力净流入占比,近60日主力净流入天数,主力挂单买入,主力挂单卖出,主力撤单买入,主力撤单卖出,连续上涨天数,连红天数,火箭发射,高台跳水,涨停封板,跌停封板,涨停开板,跌停开板,涨幅达到3%,跌幅达到3%,创20日新高,创20日新低,盘后最新价,盘后涨跌幅,指数平滑异同移动平均,DIF快线,随机指标K值,随机指标D值,随机指标J值,6周期相对强弱指标,12周期相对强弱指标,抛物线转向指标,布林中轨,布林上轨,布林下轨,5周期移动平均,10周期移动平均,20周期移动平均,60周期移动平均,120周期移动平均,250日均线,5日乖离率,36日乖离,14周期顺势指标,26周期能量指标,12周期心理线指标,近1分钟涨跌幅,近3分钟涨跌幅,MACD多头金叉信号,MACD空头死叉信号,5分钟涨跌幅,5日涨跌幅,10日涨跌幅,20日涨跌幅,60日涨跌幅,120日涨跌幅,250日涨跌幅,年初至今涨跌幅,上市以来涨跌幅,近3年涨跌幅,近5年涨跌幅,近10年涨跌幅,流通份额,最新净值,上期净值,累计净值,最新净值增长率,年初以来净值增长率,成立以来净值增长率,近一周净值增长率,近一月净值增长率,近一季净值增长率,近半年净值增长率,近一年净值增长率,近两年净值增长率,近三年净值增长率,近五年净值增长率,贴水率,基金规模,七日年化收益率,万份基金收益,IOPV,流通市值,涨停价,跌停价 默认值："最新交易日,交易时间,最新成交价,前收盘价,今日开盘价,今日最高价,今日最低价,成交量"。 |

**返回内容**

返回一个或多个场内交易基金（ETF/LOF）当前时刻的行情指标快照值（时点数据、非时间序列）。

**调用示例**

```json
{
  "name": "get_fund_price_indicators",
  "arguments": {
    "windcode": "510300.SH,588200.SH",
    "indexes": "最新交易日,交易时间,最新成交价,成交量,涨跌幅,IOPV"
  }
}
```

---

## `get_fund_performance`

返回指定基金的净值、业绩度量与评价数据，覆盖三族。净值族：单位净值、累计净值与复权净值的序列及净值增长，净值是基金资产的单位估值，为场外基金的唯一价格口径，场内基金亦适用。业绩度量族：收益率（如区间收益率、年化收益率、绝对收益口径）与同类排名（如按多周期回报、按规模）；货币基金以万份收益、7 日年化收益率为专项业绩口径。评价族：风险调整指标（如 Alpha、Beta、夏普比率、最大回撤、波动率、跟踪误差）；能力归因指标（如选股能力、选时能力）；风格分析（如风格箱、风格暴露）；基金评级（为第三方评价结果，非净值派生指标）；ETF/LOF 专项指标（如折溢价率、IOPV、净流入额）。不含基金财务报表数据。

> **适用场景：** 查询基金收益、排名和风险调整业绩。

**输入参数**

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | :---: | --- |
| question | string | 是 | 自然语言查询要求：基金实体（名称或基金代码）+ 业绩/净值/评价指标（区间收益、年化收益、同类排名、Alpha/Beta/夏普、最大回撤、跟踪误差、ETF折溢价/IOPV等）+ 时间范围。示例：查询易方达蓝筹精选近一年的收益率和最大回撤。 |

**返回内容**

返回指定基金的净值、业绩度量与评价数据，覆盖三族。

**调用示例**

```json
{
  "name": "get_fund_performance",
  "arguments": {
    "question": "查询易方达蓝筹精选混合近一年收益率、同类排名和最大回撤"
  }
}
```

---

## `get_fund_financials`

返回指定基金的财务报表与分红数据，时间维度为报告期，包括：利润指标（如基金利润、份额利润）；收入与费用（如利息收入、投资收益、公允价值变动、管理费、托管费）；报告期口径的资产净值；分红记录（如分红次数、单位分红、分红总额、分红条款）。不含净值序列与业绩评价数据。

> **适用场景：** 查询基金财务和分红。

**输入参数**

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | :---: | --- |
| question | string | 是 | 自然语言查询要求：基金实体 + 财务/分红指标（基金利润、份额利润、资产净值、利息/投资收益、管理费、托管费、分红等）+ 报告期。示例：查询易方达蓝筹精选(005827.OF)2024年的基金利润和管理费。 |

**返回内容**

返回指定基金的财务报表与分红数据，时间维度为报告期，包括：利润指标（如基金利润、份额利润）；收入与费用（如利息收入、投资收益、公允价值变动、管理费、托管费）；报告期口径的资产净值；分红记录（如分红次数、单位分红、分红总额、分红条款）。

**调用示例**

```json
{
  "name": "get_fund_financials",
  "arguments": {
    "question": "查询易方达蓝筹精选混合基金2024年年报的管理费、托管费和分红情况"
  }
}
```

---

## `get_fund_company_info`

返回指定基金的管理人（基金管理公司）档案，为以基金为入口的引用实体查询，包括：公司基本信息（如名称、成立日期、注册资本、管理层）；基金经理团队（如人数、人均管理产品数、任职年限统计）；在管规模（如合计规模及排名、非货币规模、旗下基金数量）；公司层面资产配置。注意本工具返回的是管理人公司而非基金产品本身的数据，基金产品属性请使用其他基金属性查询工具。

> **适用场景：** 查询基金管理公司。

**输入参数**

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | :---: | --- |
| question | string | 是 | 自然语言查询要求：基金实体（名称或基金代码）+ 管理人公司档案项（在管合计规模与排名、非货币规模、基金经理团队、公司层面资产配置）。本工具返回管理人公司而非基金产品本身。示例：查询易方达蓝筹精选管理人的在管合计规模与基金经理人数。 |

**返回内容**

返回指定基金的管理人（基金管理公司）档案，为以基金为入口的引用实体查询，包括：公司基本信息（如名称、成立日期、注册资本、管理层）；基金经理团队（如人数、人均管理产品数、任职年限统计）；在管规模（如合计规模及排名、非货币规模、旗下基金数量）；公司层面资产配置。

**调用示例**

```json
{
  "name": "get_fund_company_info",
  "arguments": {
    "question": "查询易方达基金管理有限公司的成立日期、管理规模和基金经理团队情况"
  }
}
```

---

# 指数工具

## `get_index_basicinfo`

获取指数基本档案信息，涵盖指数名称、发布机构、基日、基点、计算方法、成份股、成分数量、以及指数分类。

> **适用场景：** 查询指数档案。

**输入参数**

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | :---: | --- |
| question | string | 是 | 自然语言查询要求：指数实体（名称或指数代码）+ 需要的档案项（全称、发布机构、基日与基点、指数风格、成份数量、全收益/净收益指数代码、被跟踪的基金列表与数量等）。静态或准静态档案，通常无需时间限定。示例：查询沪深300的发布机构、基日基点与成份数量。 |

**返回内容**

返回指数基本档案信息，涵盖指数名称、发布机构、基日、基点、计算方法、成份股、成分数量、以及指数分类。

**调用示例**

```json
{
  "name": "get_index_basicinfo",
  "arguments": {
    "question": "查询沪深300指数的基本信息，包括发布机构、基日和成份股数量"
  }
}
```

---

## `get_index_quote`

返回指定指数的分钟级点位序列，每条记录代表一分钟。时间范围由 begin/end 指定（含首尾），未指定默认最新交易日；单日约 240 条，跨日体积按天数放大，长区间建议改用 K 线工具的聚合周期。仅交易日有数据，非交易日返回空结果。当前时刻截面状态请用行情快照工具。

> **适用场景：** 查询指数分钟行情。

**输入参数**

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | :---: | --- |
| windcode | string | 是 | 指数名称或者指数代码，如沪深300或000300.SH |
| begin | string | 否 | 开始日期，格式 yyyy-MM-dd，如 2026-03-25；未指定默认最新交易日。不可只传 end 不传 begin。 |
| end | string | 否 | 结束日期，格式 yyyy-MM-dd，如 2026-03-25；未指定默认最新交易日（即只传 begin 时返回 begin 至最新交易日的区间）。 |
| count | integer | 否 | 在开始/结束日期区间内取数的条数(整数):正数从头往后取N条，负数从结束日期往前取N条，0取全部；不会超出日期区间，默认0 |

**返回内容**

返回指定指数的分钟级点位序列，每条记录代表一分钟。

**调用示例**

```json
{
  "name": "get_index_quote",
  "arguments": {
    "begin": "2026-07-27",
    "end": "2026-07-27",
    "windcode": "000300.SH"
  }
}
```

---

## `get_index_kline`

返回指定指数在给定时间范围内的聚合点位序列（K 线），聚合周期由 period 指定（分钟级至年，默认日 K）。每条记录代表一个周期，包含开盘价、收盘价、最高价、最低价等。当日盘中分钟走势建议用分钟级行情工具（缺省即最新交易日）；相对窗口统计与技术指标不在本工具范围。

> **适用场景：** 查询指数或板块 K 线。

**输入参数**

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | :---: | --- |
| windcode | string | 是 | 指数名称或者指数代码，如沪深300或000300.SH |
| begin_date | string | 是 | 开始日期：格式yyyy-MM-dd，如2026-03-25. |
| end_date | string | 是 | 结束日期，格式为yyyy-MM-dd,如2026-03-26. |
| period | string | 否 | K线周期：默认10(日K)。可选值：1=1分钟，3=5分钟，4=10分钟，5=15分钟，6=30分钟，7=60分钟，8=120分钟，9=240分钟，10=日K，11=周K，12=月K，13=年K，14=季K，15=半年K。常用：日K=10，周K=11，月K=12 |
| count | integer | 否 | 在开始/结束日期区间内取数的条数(整数):正数从头往后取N条，负数从结束日期往前取N条，0取全部；不会超出日期区间，默认0 |

**返回内容**

返回指定指数在给定时间范围内的聚合点位序列（K 线），聚合周期由 period 指定（分钟级至年，默认日 K）。

**调用示例**

```json
{
  "name": "get_index_kline",
  "arguments": {
    "begin_date": "2026-07-20",
    "end_date": "2026-07-27",
    "windcode": "000300.SH",
    "period": "10"
  }
}
```

---

## `get_index_price_indicators`

获取一个或多个指数当前时刻的行情指标快照值（时点数据、非时间序列）。指标经 indexes 指定，取值须逐字取自其可选值清单（如 最新成交价、今日开盘价、涨跌幅、成交额等）；不填 indexes 时默认返回：最新交易日、交易时间、最新成交价、前收盘价、今日开盘价、今日最高价、今日最低价、成交量。仅返回时点截面，不含过程序列。用户询问指数的当前/最新点位或某个行情指标值时使用。

> **适用场景：** 查询指数当前指标。

**输入参数**

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | :---: | --- |
| windcode | string | 是 | 一个或多个指数名称或指数代码，多个代码用英文逗号分隔，如000300.SH,000905.SH。 |
| indexes | string | 否 | 行情指标字段，多个字段用逗号分隔。可选值：最新交易日,交易时间,中文简称,最新成交价,前收盘价,今日开盘价,今日最高价,今日最低价,最新均价,涨跌,涨跌幅,成交量,成交额,交易状态,近1分钟成交额,近3分钟成交额,近5分钟成交额,换手率,量比,振幅,基于Wind算法的量比,上市日期,现量,现额,当日主力净流入额,当日主力净流入占比,近5日主力净流入额,近5日主力净流入占比,近5日主力净流入天数,近10日主力净流入额,近10日主力净流入占比,近10日主力净流入天数,近20日主力净流入额,近20日主力净流入占比,近20日主力净流入天数,近60日主力净流入额,近60日主力净流入占比,近60日主力净流入天数,连续上涨天数,连红天数,指数平滑异同移动平均,DIF快线,随机指标K值,随机指标D值,随机指标J值,6周期相对强弱指标,12周期相对强弱指标,抛物线转向指标,布林中轨,布林上轨,布林下轨,5周期移动平均,10周期移动平均,20周期移动平均,60周期移动平均,120周期移动平均,250日均线,5日乖离率,36日乖离,14周期顺势指标,26周期能量指标,12周期心理线指标,近1分钟涨跌幅,近3分钟涨跌幅,MACD多头金叉信号,MACD空头死叉信号,5分钟涨跌幅,5日涨跌幅,10日涨跌幅,20日涨跌幅,60日涨跌幅,120日涨跌幅,250日涨跌幅,年初至今涨跌幅,上市以来涨跌幅,近3年涨跌幅,近5年涨跌幅,近10年涨跌幅,近20年涨跌幅,市净率,市净率(LF),市盈率(TTM),市盈率(预测),总市值1,流通市值,52周最高,52周最低,上涨家数,下跌家数,平盘家数 默认值："最新交易日,交易时间,最新成交价,前收盘价,今日开盘价,今日最高价,今日最低价,成交量"。 |

**返回内容**

返回一个或多个指数当前时刻的行情指标快照值（时点数据、非时间序列）。

**调用示例**

```json
{
  "name": "get_index_price_indicators",
  "arguments": {
    "windcode": "000300.SH,000905.SH",
    "indexes": "最新交易日,交易时间,最新成交价,成交量,涨跌幅"
  }
}
```

---

## `get_index_technicals`

返回指定指数基于历史行情序列计算的派生指标，包括相对窗口统计（如区间涨跌幅、均线、波动）与量价资金流向指标（如 MFI）。不含原始点位序列与实时快照。

> **适用场景：** 查询指数技术指标。

**输入参数**

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | :---: | --- |
| question | string | 是 | 获取指数技术指标及衍生指标，涵盖多周期涨跌幅（N日、周、月、年、区间）、趋向指标、反趋向指标、能量指标、量价指标、压力支撑指标、成交量指标、超买超卖指标、摆动指标、强弱指标、以及波动指标 |

**返回内容**

返回指定指数基于历史行情序列计算的派生指标，包括相对窗口统计（如区间涨跌幅、均线、波动）与量价资金流向指标（如 MFI）。

**调用示例**

```json
{
  "name": "get_index_technicals",
  "arguments": {
    "question": "查询沪深300指数的技术指标，包括MACD、RSI和20日涨跌幅"
  }
}
```

---

## `get_index_fundamentals`

返回指定指数的基本面与估值指标，按成份股加权聚合口径（如加权 PE、PB、股息率）。成份股个股的财务明细需以个股代码查询相应实体的数据；不含非预定义口径的临时聚合计算。

> **适用场景：** 查询指数成分股加权基本面。

**输入参数**

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | :---: | --- |
| question | string | 是 | 自然语言查询要求：指数实体（名称或指数代码）+ 加权基本面/估值指标 + 时间（可选：加权财务指标填报告期如2024-12-31，缺省最新披露期；估值指标及其分位数填交易日，缺省最新交易日）。示例：查询沪深300的PE、PB和成份股加权营收。 |

**返回内容**

返回指定指数的基本面与估值指标，按成份股加权聚合口径（如加权 PE、PB、股息率）。

**调用示例**

```json
{
  "name": "get_index_fundamentals",
  "arguments": {
    "question": "查询沪深300指数2024年的基本面数据，包括PE、PB和成份股加权营收"
  }
}
```

---

# 债券工具

## `get_bond_basicinfo`

返回指定债券的静态档案数据，包括发行要素（如发行规模、票面利率、期限、起息与到期日期）、信用评级、债券类型等。不含行情与估值数据、发债主体信息。

> **适用场景：** 查询债券档案。

**输入参数**

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | :---: | --- |
| question | string | 是 | 自然语言查询要求：债券实体（债券简称或债券代码）+ 需要的档案项（债券类型、发行规模、发行价格、票面利率、期限、起息与到期日期、兑付安排、债项信用评级、跨市场代码等）。静态或准静态档案，通常无需时间限定。示例：查询国债2601的发行规模、票面利率与到期日期。 |

**返回内容**

返回指定债券的静态档案数据，包括发行要素（如发行规模、票面利率、期限、起息与到期日期）、信用评级、债券类型等。

**调用示例**

```json
{
  "name": "get_bond_basicinfo",
  "arguments": {
    "question": "查询国债2601的基本信息，包括发行日期、票面利率和期限"
  }
}
```

---

## `get_bond_market_data`

返回指定债券在指定时间范围内的区间行情与估值数据，频率支持日、周、月、年及自定义区间，包括价格、收益率、估值指标等。

> **适用场景：** 查询债券行情与估值。

**输入参数**

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | :---: | --- |
| question | string | 是 | 自然语言查询要求：债券实体（债券简称或债券代码）+ 行情/估值指标（收盘价、成交量、到期收益率、估价收益率、久期、凸性、利差等）+ 时间范围与频率（可选，日/周/月/年或自定义区间，缺省最新交易日、日频）。示例：查询国债2601最新一日的到期收益率、久期和凸性。 |

**返回内容**

返回指定债券在指定时间范围内的区间行情与估值数据，频率支持日、周、月、年及自定义区间，包括价格、收益率、估值指标等。

**调用示例**

```json
{
  "name": "get_bond_market_data",
  "arguments": {
    "question": "查询国债2601的行情数据，包括久期、凸性和收益率"
  }
}
```

---

## `get_bond_issuer_info`

返回指定债券发债主体的档案信息，包括主体基本信息、主体信用评级等。债券属性与主体财务数据请使用相应工具。

> **适用场景：** 查询债券发行主体。

**输入参数**

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | :---: | --- |
| question | string | 是 | 自然语言查询要求：债券实体（债券简称或债券代码）+ 主体档案项（主体名称、注册地址、行业分类、股权结构、企业背景、主体信用评级等）。示例：查询22万科MTN001发债主体的注册地址、行业分类与最新主体评级。 |

**返回内容**

返回指定债券发债主体的档案信息，包括主体基本信息、主体信用评级等。

**调用示例**

```json
{
  "name": "get_bond_issuer_info",
  "arguments": {
    "question": "查询国债2601发债主体的公司信息，包括注册地址和行业分类"
  }
}
```

---

## `get_bond_financial_data`

返回指定债券发债主体的财务数据，时间维度为报告期。主体档案信息与债券本身属性请使用相应工具。

> **适用场景：** 查询发行主体财务。

**输入参数**

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | :---: | --- |
| question | string | 是 | 自然语言查询要求：债券实体（债券简称或债券代码，返回其发行人的财务数据）+ 财务指标（营收、利润、资产、负债及财务比率等）+ 报告期（可选，填如2024-12-31，缺省最新披露期）。示例：查询22万科MTN001发债主体2024-12-31的营业收入和净利润。 |

**返回内容**

返回指定债券发债主体的财务数据，时间维度为报告期。

**调用示例**

```json
{
  "name": "get_bond_financial_data",
  "arguments": {
    "question": "查询国债2601发债主体2024年营业收入和净利润"
  }
}
```

---

# 信息与宏观工具

## `get_company_announcements`

检索并返回上市公司公告文本，支持按公司、公告类型、时间范围等条件检索。公告对应的结构化字段（如分红金额、事件日期）需查询相应实体的数据；第三方媒体报道不在本工具范围。

> **适用场景：** 检索官方公告与监管披露。

**输入参数**

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | :---: | --- |
| query | string | 是 | 自然语言检索要求：检索目标（某主体、某事项的官方披露原文）+ 可选线索——公告类型（年报、季报、分红、招募说明书等）、时间范围。示例：查询贵州茅台2026年以来的分红派息相关公告。 |
| top_k | integer | 否 | 返回的相关文档或片段的最大数量 默认值：5。 最小值：1。 |

**返回内容**

检索并返回上市公司公告文本，支持按公司、公告类型、时间范围等条件检索。

**调用示例**

```json
{
  "name": "get_company_announcements",
  "arguments": {
    "top_k": 3,
    "query": "贵州茅台最新公告"
  }
}
```

---

## `get_financial_news`

检索并返回财经新闻文本，支持按主题、实体、时间范围等条件检索。不包含发行人官方公告与券商研报；新闻中提及的结构化数据（如价格、财务数值）以相应实体的数据返回为准。

> **适用场景：** 检索第三方财经媒体报道。

**输入参数**

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | :---: | --- |
| query | string | 是 | 自然语言检索要求：检索目标（主题、事件，无需必含具体实体）+ 可选线索——实体、行业、时间范围。示例：近一周美联储利率政策新闻。 |
| top_k | integer | 否 | 返回的相关文档或片段的最大数量 默认值：5。 最小值：1。 |

**返回内容**

检索并返回财经新闻文本，支持按主题、实体、时间范围等条件检索。

**调用示例**

```json
{
  "name": "get_financial_news",
  "arguments": {
    "top_k": 3,
    "query": "贵州茅台最新一周的新闻"
  }
}
```

---

## `get_financial_data`

根据自然语言描述执行结构化数据的计算与提取，支持自定义指标组合、跨实体聚合与灵活的数据加工。仅当所需结果不能由任何预定义数据工具直接返回时使用本工具：单一实体的属性数据、指数与板块的预定义加权指标、宏观指标均调用相应数据服务；按条件筛选并返回实体列表使用各服务的筛选工具（本工具返回计算结果而非实体列表）。

> **适用场景：** 执行跨实体聚合计算。

**输入参数**

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | :---: | --- |
| question | string | 是 | 自然语言计算/取数要求：实体范围 + 指标或计算逻辑 + 日期/时间范围或报告期。用于跨标的聚合、加权平均、排名、复合指标推导等预定义工具无法直接返回的结构化结果。示例：查询中国A股市场过去一年的平均成交量。 |

**返回内容**

根据自然语言描述执行结构化数据的计算与提取，支持自定义指标组合、跨实体聚合与灵活的数据加工。

**调用示例**

```json
{
  "name": "get_financial_data",
  "arguments": {
    "question": "查询中国A股市场过去一年的平均成交量"
  }
}
```

---

## `natural_language_get_edb_data`

查询 Wind EDB 宏观经济指标:按自然语言问句搜索匹配指标(返回代码、名称、单位),或按指标代码提取历史时间序列。question 搜索时填问句(如"上海GDP"),提数时填指标代码(如 G0000069,多个逗号分隔);时间用 beginDate/endDate 或 observation 指定。需统一口径对比(跨国/跨地区/跨指标、排名、看板、差值)或用户明确要求换算时,再用 targetMagnitude、targetFrequency、targetCurrency 设置目标量级、频率、币种。不回答概念、定义等无需访问 EDB 数据的问题。

> **适用场景：** 当用户查询宏观经济指标或 Wind EDB 时间序列时使用。仅想找指标用“仅搜索”；已知 EDB 代码直接取数用“仅提数”；自然语言找指标并返回数值用“搜索并提数”。取数时显式传日期或期数。

> **适合的问法：** “帮我搜索中国GDP相关的Wind EDB指标。”

**输入参数**

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | :---: | --- |
| executionMode | string | 是 | 执行方式。仅搜索：用户只想查找、筛选或推荐宏观经济指标，不需要返回具体数值。仅提数：用户已经给出明确指标代码，需要直接提取数据。搜索并提数：用户用自然语言描述指标并要求返回具体数据，需要先搜索指标再提数。 |
| question | string | 是 | 查询内容。执行方式为【仅提数】时，填入指标代码，多个代码用英文逗号分隔，如 G0000069,G8411182。执行方式为【仅搜索、搜索并提数】时，填入指标或经济数据的自然语言描述，如 中国GDP、上海CPI、出口相关指标。注意：question 主要用于指标搜索或指标代码提数，不应依赖工具从 question 中自动解析日期；时间范围应通过 beginDate/endDate 或 observation 显式传入。 |
| beginDate | string | 否 | 数据提取开始时间，格式为yyyyMMdd，如20230101。与observation参数互斥，不应同时填写。如近三年数据，则按照当前时间填写。 |
| endDate | string | 否 | 数据提取结束时间，格式为yyyyMMdd，如20241231。与observation参数互斥，不应同时填写。如近三年数据，则按照当前时间填写。 |
| observation | integer | 否 | 观测期数。用户要求提取近 N 期数据时填入整数，如“近10期”填10；与beginDate和endDate参数互斥，不应同时填写。返回结果时间返回会对齐最低频次指标时间，如近10期数据，年频指标返回10条数据，季频指标返回40条数据。 |
| targetMagnitude | string | 否 | 目标数量级或展示单位,如 元、亿元、万亿元、十亿、百万吨。两种情况需填写:1.用户明确提出单位转换/展示要求,如'用万亿/亿/十亿/百万吨展示';2.用户语境存在潜在数量级对齐意图(即使未明说),如跨指标拼表、同屏看板、差值计算、排名、多经济体 GDP 体量对比、单指标按指定量级换算(如'日本GDP折算成亿美元')等需统一展示量级的场景。用户未提数量级且无对齐意图时不填。 可选值：十万分之一、万分之一、千分之一、百分之一、十分之一、个、十、百、千、万、十万、百万、千万、亿、十亿、百亿、千亿、万亿、十万亿、百万亿、千万亿、亿亿、十亿亿、百亿亿。 |
| targetCurrency | string | 否 | 目标币种,填 ISO 4217 三字母代码,如 CNY(人民币)、USD(美元)、EUR(欧元)、JPY(日元)、KRW(韩元)、INR(印度卢比)。两种情况需填写:1.用户明确要求折算、统一币种或指定汇率,如'折美元''用人民币算';2.用户语境存在潜在币种对齐意图(即使未明说),如中美 GDP 对比、中美日 GDP 排名、G7 政府债务从高到低排序、跨国家经济体量比较、单指标跨国换算(如'日本GDP折美元')等。若用户未指定币种但任务要求可比,按业务默认可比币种(通常 USD)填写并在回答中说明口径。同一经济体单一指标查询且无跨国可比需求时不填。 |
| targetFrequency | string | 否 | 目标频率。两种情况需填写:1.用户明确要求按日、周、月、季、年展示或重采样,如'按季度展示''汇总成年度';2.用户语境存在潜在频率对齐意图(即使未明说),如把月度指标汇总成季度、年度数据拆分为季度或月度、不同频率指标整合成一张季度表、比较同一时间粒度下的 GDP/PMI/CPI 等。用户未提频率且无对齐意图时不填。 可选值：日、工作日、周、月、季、半年、年、年度。 |

**返回内容**

查询 Wind EDB 宏观经济指标:按自然语言问句搜索匹配指标(返回代码、名称、单位),或按指标代码提取历史时间序列。

**调用示例**

```json
{
  "name": "natural_language_get_edb_data",
  "arguments": {
    "question": "中国GDP",
    "executionMode": "仅搜索"
  }
}
```

---
